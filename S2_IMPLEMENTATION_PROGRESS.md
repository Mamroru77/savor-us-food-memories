> **后续批次更新**：本文保留S2文字共享批次记录。S3已新增主动共享照片及受保护媒体生命周期候选，见 S3_MEDIA_IMPLEMENTATION.md；仍未部署/真机验收。

# S2：双人空间、文字共享/独立评分、共同 Wishlist

2026-09-14。策略已改为**连续完成业务代码，最后统一 Codex/真机验收**。本批未部署、未推送、未操作用户Windows工程。

## 已接入的候选能力

- 新增 `spaces` 云函数：所有行为通过真实 getWXContext 和产品身份比对授权。
- 事务创建邀请、接受邀请、同步两账号关系及退出空间；24小时高熵邀请、不自动绑定、不暴露邀请方私人资料。
- 邀请撤销/过期/一次消费、并发接受只有一个成功、已绑定用户不能再绑定、退出双方同时解除后续访问。
- 每次共享读写检查当前活跃空间和空间成员版本。过去 shared 标记不授予共享权限，旧空间不会迁移给新成员。
- 仅主人可主动发布本人云餐食的允许文字字段；不能发布别人的记录。照片、历史评分、OPENID/成员凭证均不复制。
- 当前两成员分别以真实产品身份评分/撤回自己的评分；不能由一个评分推导另一个评分，不能伪造对方评分。
- 主人可停止共享；重新共享先读取本人发布版本，再明确同意，不由旧重试自动重新公开。
- Wishlist 新建、本人想去/再看/不考虑、共同标记吃过/重新打开、创建者删除、文本转自己的空私人草稿。
- 共享操作先持久化到当前私人分区的 spaceIntent，丢响应/重启后显式重试；不跨账号或新空间重放。非确定失败不删除操作身份。
- 新增非Tab的 space 页面，Us 和 Me 提供入口；保留原五Tab及Map样式。
- 共享列表不落地到私人store或持久缓存；15秒显示租约，隐藏页面或身份变化清空，网络失败不保留旧列表。

## 实现边界

1. 本批不是共享照片授权实现。共享内容明确不包含私有照片ID；下一批补媒体引用与授权链路，不以公开存储替代。
2. 退出后服务端立即拒绝后续请求；已经返回的文字最多展示至15秒租约到期。无法撤回已经看到/截取/导出的信息。
3. 当前每空间最多50个 Wishlist 索引和50个共享餐食索引（含历史删除/撤回索引），达到上限明确返回 SPACE_LIMIT；不是无限数据容量承诺。
4. 邀请生成限频，新邀请至少间隔60秒；高熵token属于敏感邀请凭据，不写日志、不公开分享。
5. 待确认操作只有一个；必须先显式重试确认，不能把“队列里没了”当远端成功。冲突需刷新后重新操作。
6. 实际CloudBase事务限制、集合权限、客户端成员同步及原生选择器时序尚待最终统一验证。测试中的串行事务模型不能替代真实SDK验证。
7. 旧Us页面的本机显示和昵称设置仍保留；真实空间以新的“管理双人空间与Wishlist”入口为准。云资料/真实成员昵称同步属于后续业务。

## 文件

- cloudfunctions/spaces/{handler,index}.js
- miniprogram/utils/{spaces,spaceCopy}.js
- miniprogram/pages/space/*
- identity.js 新增按本人分区保存的共享操作意图。
- tools/verify-spaces.cjs：26个实际domain/client源码的mock场景。

## 测试

`npm run verify:spaces`：26/26。
`npm run verify:all`：最新完整结果见 CURRENT_TEST_RESULTS.txt。
新增测试没有替代S1的22项身份测试和原业务233项；二者继续执行。
