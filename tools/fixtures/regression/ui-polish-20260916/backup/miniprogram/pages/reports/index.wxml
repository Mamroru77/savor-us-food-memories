<view class="tools {{dusk ? 'dusk' : ''}}"><s-identity-gate /><view wx:if="{{!locked}}"><text class="eyebrow">SAVOR / YOUR YEAR</text><text class="title">年度报告与里程碑</text><text class="hint">仅统计当前账号本机真实日记；不读取伴侣资料。排除示例、删除、待删除和未来记录。未同步的其他设备记录不会自动出现。</text><picker mode="selector" range="{{years}}" bindchange="selectYear"><view class="field">{{year}} · 选择年份</view></picker><view class="card" wx:if="{{report}}"><text class="heading">{{report.year}} · {{report.count}} 餐记录</text><text class="hint">截至 {{report.asOf}} · {{report.days}} 个记录日 · {{report.places}} 个可确认地点 · 最长连续 {{report.longestStreak}} 天</text><text class="hint">{{report.unknownLocations}} 条未知地点不计入地点数。单评分样本 {{report.ratedCount}} 条；不推算双人评分或总消费。</text><view wx:for="{{report.milestones}}" wx:key="count" class="row">累计第 {{item.count}} 餐 · {{item.date}}</view><view wx:if="{{!report.milestones.length}}" class="row">慢慢记录，不必赶进度。</view></view><canvas type="2d" id="report-canvas" class="poster" /><view class="card"><text class="heading">主动选择分享范围</text><text class="hint">海报固定只含汇总。HTML 默认仅汇总；启用下面的选项才包含餐厅、日期与已确认城市。不包含姓名、笔记和照片。</text><view class="row"><switch checked="{{details}}" bindchange="details" color="#809176" aria-label="在HTML中加入餐厅与已确认城市" /> HTML 加入餐厅与已确认城市</view><button disabled="{{busy}}" bindtap="exportHtml">导出 / 分享 HTML 报告</button><button disabled="{{busy}}" bindtap="sharePoster">预览 / 分享仅汇总海报</button><text class="hint">分享后无法撤回对方保存的副本。</text></view><text wx:if="{{message}}" class="notice" selectable>{{message}}</text></view></view>
