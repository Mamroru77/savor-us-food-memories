> 最新范围修订：大众点评为堂食主来源，美团及其他外卖平台用于外卖。点评短链、美团堂食、失效／团购页等不再是首批必备样本；异常输入仍需安全失败。本地文案导入现已实施，见 SHARE_IMPORT_UPDATE.md；本文件其余阶段为原路线图，不代表全部已上线。

# Savor：步骤 6 与剩余能力规划

**规划日期：2026-09-11｜基于现有代码 `75c11d3`｜本轮仅规划，不实施新业务**

## 一、已确认方向与范围

用户本轮已确认：

1. **独立账号 + 双人共享空间**：双方各自拥有私人记录，主动共享的内容进入 Us。
2. **链接导入优先**：第一期针对大众点评／美团分享链接，先提取候选信息，再由用户确认。
3. **双人共同 Wishlist**：双方添加想吃的店、表达各自意愿，吃过后转为餐食草稿。

继续保留：原生微信小程序 / CloudBase / 腾讯选点、best-ui 五个 Tab、Nocturne、个性化、Sheet、Toast、照片与本地草稿。万能导入入口仍放在 Add。**不新增第六个 Tab，不重写 Home / Add，不把 Wishlist 伪装成餐食记录。**

下文的权限、解绑、字段共享及保留期属于**建议默认规则**，不是用户已经逐项确认的决定。开发前应把这些规则冻结为验收标准。

---

## 二、先澄清当前状态：不能把旧审计当成现状

现有 1–5 已完成代码与本地回归：本地日期、位置元数据确认、真实统计、人均/菜系/菜品/九图/未评分、云端编辑删除和状态队列。此前交付报告为 **190 项静态检查、85 项 mock 测试通过**。

这些结果不等于已经完成微信开发者工具、两台真机、线上 CloudBase、安全规则或并发实测。本轮没有重新运行或新增功能测试。

| 项目 | 当前状态 | 后续处理 |
|---|---|---|
| 日期与基础统计 | 已有本地日期 helper、真实 selectors | 做真机跨日验收；扩展共享后的统计口径 |
| 城市与国家地区 | 保守地址解析 + 手动确认；旧记录待确认流程 | 提升解析覆盖，不臆造自动批量修复 |
| 编辑/删除/收藏等 | 已有 owner 权限、revision、outbox、tombstone | 双账号隔离、共享撤回、弱网与长期队列验收 |
| 在一起天数 | 依据现有资料日期计算 | 绑定后明确共同日期的确认规则 |
| 周报与旅程 | 已有真实周统计、共享记录时间线 | 年报、里程碑、分享报告仍未开发 |
| 双人评分 | 兼容旧评分，不复制为双方投票 | 新绑定后按真实用户身份保存独立评分 |
| 云端照片清理 | 本机引用保护；云文件暂时保留 | 新增归属、引用、保留期及安全回收流程 |
| JSON 导入 | 本机备份导入，不自动批量上传 | 后续做云端批量导入预览、去重和任务状态 |
| 资料与偏好 | 主要为本机设置 | 最小云资料先支持绑定；其余设置后续分类同步 |
| 提醒与反馈 | 本机设置/记录，非实际投递服务 | 独立建设订阅通知和反馈投递 |

**第一道门槛：先验收现有版本，再叠加双人权限。** “当前代码没有实现”与“已有实现但未线上验收”必须分开管理。

---

## 三、阶段 S0：现有版本验收 + 链接可行性验证

这两项可以并行，暂不扩大业务表写入权限。

### S0-A：现有版本发布门槛

- 部署当前 `mealRecords` 与对应前端，保留日记/草稿及 outbox，先备份数据。
- 两个微信账号、至少两台设备验证 owner 隔离：未绑定前互相不能读取/修改餐食。
- 实测离线编辑/收藏/删除 → 杀进程 → 重启 → 联网；验证操作身份不丢、删除不复活。
- 实测丢响应、并发编辑冲突、旧列表晚到、存储不足、九图上传失败及重试。
- 实测无评分、旧双评分、城市修正、跨日、Sheet 底部操作和键盘滚动。
- 补充故障可诊断信息：错误码、操作 ID、重试次数、最后成功同步时间；不记录餐食正文、原始私密链接和照片内容。

**退出标准**：没有跨账号访问或草稿/待同步数据丢失问题；已知问题有明确处理，不用 mock 代替线上验收。

### S0-B：大众点评／美团链接探针

美团官方接入指南描述了业务对接、应用审核及 OAuth 授权流程。这不能推导出存在可供本项目无条件使用的“任意消费者餐厅链接解析 API”。[1](http://open.meituan.com/docs/introduce/guide)

开发前需要一组真实分享样例，建议 20–30 条：

- 餐厅/门店长链接、短链接、带文案分享文本；
- 不同城市和同名分店；
- 过期、登录限制、仅 App 可打开的链接；
- 团购/商品页与门店页；
- 不应解析的订单、付款、账户或私人页面。

每条记录：链接类型、可否合规访问、能稳定取得的字段、字段证据、耗时、失败原因和回退结果。样例中的个人信息先脱敏。

**退出标准**：形成真实支持矩阵。不能稳定取得的信息就不列为自动能力。若平台页面不可访问，第一期仍可交付“分享文本提取 + 原链接保存 + 用户补全”，但必须明确标注为有限支持，而不是宣称完整解析。

---

## 四、阶段 S1：账号、云资料与媒体授权基础

情侣绑定之前，先解决“你是谁、缓存属于谁、照片谁能看”。

### 1. 最小云端用户资料

- 服务端通过 `getWXContext()` 识别用户；客户端不提交可作为授权依据的 OPENID。
- 服务端生成稳定的产品用户 ID，用于业务引用和缓存分区。
- 第一阶段只同步昵称、头像、资料版本等绑定必需字段；不强制手机号码或真实姓名。
- 语言、主题、减少动效等先保持设备设置，不因另一台设备修改而强制切换。
- 个性化文案、饮食偏好等可另做“账号同步”，首次迁移需选择，避免覆盖本机内容。

### 2. 缓存与 outbox 分区

保留 `savor-diary-v1` / `savor-draft-v1` 兼容入口，但在内部显式区分：

- 本人私人记录与本人待同步操作；
- 当前情侣空间的共享缓存；
- 分享、解绑等依赖成员身份的操作。

每个操作记录所属用户与空间上下文，**实际授权仍以服务端身份为准**。切换账号、解绑或加入新空间时，旧操作必须隔离，不能以新账号/新伴侣身份重放。本人私人离线编辑仍可用。

### 3. 云照片共享不能靠放宽存储规则

当前“仅创建者可读写”的照片策略不能直接满足伴侣查看。建议：

- 建立媒体归属/上传会话记录，记录服务端分配的上传路径和关联业务对象。
- 获取伴侣照片时，服务端先验证当前成员关系、餐食共享状态及资产归属，再返回短期访问能力。
- 不接受任意 `cloud://` ID 就由管理员权限签出访问地址；不把整个存储桶改成公开读。
- 旧图片先做可验证的归属迁移。无法证明归属的图片，不自动对伴侣授权；需要原作者重新上传等安全迁移路径。
- 撤回共享后立即停止新授权并清理在线缓存；已签出的链接在到期前、已离线缓存或截图的内容，**不能承诺瞬时远程抹除**。

**退出标准**：账号隔离和媒体授权有独立测试；没有可用身份时，不能提交任何共享写入。

---

## 五、阶段 S2：情侣绑定与主动共享

### 用户流程

**Us → 建立共享空间 → 创建邀请 → 复制邀请 → 对方在 Us 接受 → 双方确认 → 绑定成功。**

MVP 先做复制邀请与应用内兑换；小程序码、外部唤起等可以在平台能力验收后追加，不与绑定权限混为一谈。

### 建议默认规则

| 场景 | 建议行为 |
|---|---|
| 私人记录 | 仅作者可见、可编辑、可删除 |
| 共享餐食 | 双方可见，作者可编辑/删除；伴侣第一期只读基础内容 |
| 收藏与喜欢 | 各自独立，不能覆盖对方状态 |
| 评分 | 各自只修改自己的评分；旧命名评分不得直接冒认成新绑定用户 |
| 共享/撤回 | 由作者操作，伴侣不能把作者的私人记录自行公开 |
| 分享字段 | 首次分享展示字段预览；费用、备注、照片是否共享要明确，不默认暴露私人内容 |
| 在一起日期 | 一方提出，另一方确认，保留修改记录 |
| 绑定数量 | MVP 一人仅一个活跃双人空间 |
| 退出/解绑 | 任意一方可退出；服务端立即撤销共享读取及写入权限 |

### 必须避免的迁移事故

现有 `shared: true` 只代表过去本机/本人日记里的 Us 标记，**不是同意把历史记录发送给新伴侣**。

绑定时不自动发布历史记录。提供“选择要共享的历史餐食”预览，由作者确认目标空间和内容；未选中的记录继续私人可见。邀请码、`coupleId`、`memberOpenids` 都不能替代服务端实时成员校验。

### 邀请安全

- 服务端生成不可预测令牌，数据库仅保存摘要；建议 24 小时过期、单次兑换，可撤销。
- 不使用可无限尝试的简单六位数邀请码。
- 创建/兑换限频；禁止自邀、重复绑定、第三人加入。
- 接受邀请与成员更新使用事务/条件写入，保证并发时只出现两名有效成员。
- 旧邀请在撤销或解绑后不能恢复权限。

### 解绑与离线

- 本人餐食保留，并停止向原空间共享；原伴侣的餐食从本机共享缓存移除。
- 旧空间的离线共享操作标记为失效，不能在下一段关系中自动提交。
- 共同 Wishlist 的保留规则见下一节；不自动带入新情侣空间。
- 离线设备无法即时获知解绑。建议共享缓存有有限授权有效期；到期需联网核验。本人私人缓存不受此限制。

### 统计口径

- Home / Me：继续统计本人记录，包括本人主动共享的记录，不把伴侣记录变成本人餐食。
- Us：统计当前空间中双方主动共享的记录，按记录 ID 去重。
- 共享撤回、删除、解绑后同步调整统计。
- 共同吃过一次但双方各记了一条，不能仅凭同店同日自动合并。以后如引入共同餐次 ID，应另做明确关联。

**退出标准**：用两个真实账号完成邀请、分享、撤回、评分、解绑及重新绑定；旧缓存/旧邀请/旧离线队列均不能越权。

---

## 六、阶段 S3：大众点评／美团链接导入 MVP

### 入口与流程

**Add → 万能导入 → 粘贴分享文案或链接 → 解析 → 候选信息预览 → 用户补全与确认 → 保存餐食草稿。**

Wishlist 上线后增加“保存为想吃”目标；目标未上线时不放一个不可用的假按钮。

### 第一阶段范围

- 识别分享文本中的链接与平台，保留来源信息。
- 按 S0 验证过的链接类型提取店名、分店名、地址、可验证的来源标识等候选字段。
- 显示自动提取、低置信度及缺失字段；提供原文对照和逐项编辑。
- 抓取失败时保留已粘贴文本、链接和用户修改，转人工补全；失败也可以保存有效草稿。
- 覆盖当前 Add 草稿前确认；取消解析不清空原草稿。
- 记录任务状态和失败原因，避免重复点击生成多份草稿。

### 不自动填成用户事实

- 平台评分不等于我的评分；默认仍为未评分。
- 平台人均是价格提示，不等于本次消费金额。
- 推荐菜不等于本次吃过的菜。
- 发布日期不等于用餐日期。
- 同名门店不能自动选择分店；导入地址/坐标仍需与腾讯选点一致性确认。
- 不默认搬运第三方照片；若以后支持，需明确使用权限、来源和存储政策。

### 抓取与隐私边界

- 只允许验证过的公开链接类型；不绕过登录、验证码、付费或反爬机制。
- 不索取用户 Cookie，不抓取订单/账户页面，不保存访问凭证到日志。
- 限制协议、端口、域名、重定向次数、响应大小与超时；每次重定向重新验证目标。
- 防止内网、回环、云元数据地址及 DNS 重绑定等 SSRF；不能只做域名字符串判断。
- 外部网页与分享文本只作为待提取数据，不能作为指令驱动云端写入。
- 若后续引入模型辅助提取，必须使用结构化输出校验，仍由用户确认；是否调用第三方模型需要额外隐私与费用评估。

### 验收

输出平台支持矩阵，而不是“解析率 100%”：支持、部分支持、需补全、不支持。对登录受限、无效短链、错误分店、多个链接、超时和重复提交，都有可恢复路径。

---

## 七、阶段 S4：双人共同 Wishlist

### 放置位置

- Us：共同想吃清单入口与少量摘要。
- Add：未来可把导入候选保存为想吃。
- Map：后续添加“已吃 / 想吃”筛选，明确区分标记；不挤掉现有地点操作。
- 不增加 Tab，不把未用餐的条目算进餐食、周报或旅程。

### MVP 能力

- 手动/从导入候选添加餐厅、地址、来源链接和备注。
- 双方分别选择“想去 / 一般 / 暂不想去”；只修改自己的意愿。
- 状态为想吃、计划中、已完成、归档；先不做日历排期或多人投票系统。
- 建议双方可修正公共门店信息，修改受 revision 保护；各自私密备注独立存储。
- 建议仅创建者可归档/移除条目，另一方可表达不想去；后续再决定是否开放共同管理权限。
- 疑似重复条目提供合并建议，不按店名直接自动合并不同分店。

### “吃过了”转餐食

1. 选择条目 → 打开 Add 草稿，预填店名、位置和来源；不伪造用餐日期、金额、菜品或评分。
2. 用户补充并确认保存。
3. 只有餐食保存成功，才完成 Wishlist 状态迁移；上传失败或取消时，条目仍保留。
4. 两人同时转同一条目时，使用同一完成事件的幂等身份及服务端事务/条件更新，不能产生两个意外副本。
5. 若用户改为私人保存，不能把私人餐食内容或 ID 暴露给伴侣；建议不自动将共同条目标成全员已完成。
6. 如果保存期间解绑，拒绝旧空间提交，保留草稿；转私人保存需重新明确确认，而不是静默改共享范围。

### 解绑后的清单

建议每人保留自己创建条目的基本内容，停止原空间共享；原伴侣的意愿、私人备注和不可继续访问的内容不带入新空间。加入新关系时不自动再次共享。这一规则需在开发前最终确认。

**退出标准**：两端新增/修改/表达意愿/归档/完成均一致；断网恢复、并发完成和解绑期间提交不丢数据、不越权、不重复记餐。

---

## 八、建议的数据与接口分层

以下为设计草案，不是已创建的集合或已部署接口。

| 领域 | 建议数据 | 授权重点 |
|---|---|---|
| 账号 | users、用户资料版本 | 本人修改，伴侣只读取获准的展示字段 |
| 关系 | couples、couple_invites、成员版本 | 服务端事务与实时成员校验 |
| 共享餐食 | dining_records 的显式共享目标/共享版本 | 作者所有权 + 当前有效共享授权 |
| 个人态 | memory_user_states | 每人独立收藏/喜欢/评分；只能写自己的状态 |
| 导入 | import_jobs、候选数据与来源 | 仅发起者可读；解析不直接写餐食 |
| 想吃 | wishlist_items、各自意愿/完成事件 | 活跃成员范围，版本化更新 |
| 地点 | 受 owner/空间约束的地点引用 | 不把私人地址推到公共餐厅目录 |
| 媒体 | assets、upload_sessions、引用与删除候选 | 验证归属后授权，不凭客户端 fileID 签出资源 |

客户端继续使用统一服务入口和领域适配器；页面只使用 Memory / Wishlist / ImportCandidate 等模型，不直接写数据库。服务端可拆分领域函数，避免把网页抓取、邀请授权与餐食写入塞进一个高权限大函数。

保留现有字段兼容，但新成员关系由服务端控制。共享写入携带成员版本用于冲突检查，不能由客户端伪造成员。旧客户端的 `shared` 标记不得成为新的跨用户发布指令。

---

## 九、其余尚未实现能力：分批处理

| 优先级 | 能力 | 建议版本/处理方式 |
|---|---|---|
| P0 | 现有代码的真实 CloudBase/设备验收 | S0，阻塞双人功能上线 |
| P0 | 身份分区、成员撤权、媒体授权 | S1/S2，不能留到 UI 做完之后 |
| P1 | 邀请、主动共享、各自评分/个人态 | S2 |
| P1 | 点评/美团链接有限支持 | S0 探针 → S3，不承诺未验证类型 |
| P1 | 双人 Wishlist 与幂等转餐食 | S4 |
| P1 | 旧媒体授权迁移 | 与 S1/S2 同期；无法验证的照片不自动共享 |
| P2 | 云资料/偏好/个性化的完整同步 | 最小资料之后；主题等保留设备级选择 |
| P2 | 地点实体、多次到访归一、重复项合并 | 先提供内部引用和人工确认；不宣称全球 POI 唯一识别 |
| P2 | 更完整的地址解析/逆地理能力 | 单独确认腾讯能力、密钥保管、费用与配额 |
| P2 | 安全孤立文件回收 | 引用体系建立后：宽限期 → 候选 → 二次校验 → 删除日志；未知结果的上传不得回收 |
| P2 | JSON/表格云端批量导入 | 预览、去重、旧字段映射、明确上传确认、逐条失败恢复 |
| P2 | 截图 OCR | 复用 ImportCandidate，不另建一条绕过确认的写库路径 |
| P2 | 年报、真实里程碑、报告分享 | 基于已验证统计；另定义边界、图片授权和生成成本 |
| P2 | 服务端提醒 | 先确认微信订阅能力与用户授权；本机开关不代表已订阅 |
| P2 | 反馈投递 | 最小可用工单/投递状态，默认不携带餐食和照片 |
| P2 | 账号注销/数据清除/彻底删除 | 区分本人记录、共同内容、媒体与法定留存；先有明确政策 |
| P3 | 自动路线、智能推荐、更多内容平台 | 首轮不做，避免范围失控 |

**物理删除与逻辑删除不同。** 现有 tombstone 已解决正常同步删除，不代表云文件被立即擦除；后续应提供清晰保留期和用户数据清除路径。

---

## 十、执行顺序与上线门槛

| 阶段 | 交付物 | 前置依赖 | 上线门槛 |
|---|---|---|---|
| S0 | 当前版本实测清单 + 点评/美团支持矩阵 | 开发者工具/云权限、两账号两设备、真实链接样例 | 不存在数据丢失/越权阻断项 |
| S1 | 最小云资料、缓存隔离、媒体授权设计与实现 | S0 安全结论 | 私人数据不能被共享接口绕过 |
| S2 | 绑定、历史记录主动分享、撤回、解绑、个人态 | S1 | 完整双账号权限和离线生命周期通过 |
| S3 | 链接 → 候选 → 确认 → Add 草稿 | S0 链接结论；服务边界 | 不支持类型可安全降级，无静默写库 |
| S4 | 双人 Wishlist 与转餐食 | S2；S3 可提供导入来源 | 并发完成不重复、解绑不泄露 |
| S5 | 媒体回收、批量导入、OCR、完整设置同步等 | 按上表逐项排期 | 各自有测试及回滚方案，不捆绑全部上线 |

建议逐阶段提交和验收，不一次把步骤 6 所有候选能力写完再测试。当前没有真实平台样例与部署实测结果，不承诺固定交付日期；完成 S0 后再给出按工作量和外部依赖区分的排期。

### 进入开发前仍需冻结的规则

1. 伴侣第一期是否只读基础餐食；本规划建议只读，评分/收藏各自独立。
2. 分享预览中哪些字段默认包含，尤其费用、备注及照片。
3. 解绑后共同 Wishlist 的归属与共享缓存授权有效期。
4. 点评/美团的真实样例、可用授权或接口资质；不可解析时是否接受有限支持版本。
5. 旧照片的归属迁移、云文件保留期以及之后的数据清除政策。

这些是下一阶段的产品与安全决策，不应由实现代码暗中决定。

---

## 十一、建议下一步

先执行 **S0：现有版本双账号/双设备验收 + 大众点评/美团分享链接样例验证**，同时冻结邀请、共享和解绑规则；再进入 S1/S2 开发。

本规划未修改业务代码、未创建集合、未部署新云函数，也未更新当前代码交付包。现有最新实现仍为 `75c11d3`；新阶段不能宣称已通过其旧测试。
