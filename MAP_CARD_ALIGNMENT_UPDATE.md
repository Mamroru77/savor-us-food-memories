# 地图名称下置与卡片方图调整

> 最新决定：用户在 A–D 示意图中选择 D。当前地图已不显示店名，名称只在底部卡片／收起小条／附近记录列表中出现，聚合角标只显示数量；旧名称牌和额外碰撞留白已移除。方形图片与卡片内部收起按钮保持。当前验证 193/129；以下名称牌说明为上一轮历史记录。

基线：`71baae3`。本轮依据用户红色箭头调整 Map，不涉及 Me、云函数、真实坐标或餐食数据。

## 已实施

1. **收起按钮**从卡片外侧右上角移到左栏内容区右下角、图片左侧：右距 326rpx、底距 20rpx。与底部位置操作分别预留宽度，不叠放。
2. **所选店名**由图标上方 callout 改为下方原生 label，距地图锚点向下 10px，约按文字宽度居中。字号从原来的 10px 提升为 13px。
3. 名称使用明暗主题分别适配的半透明圆角底板，文字不模糊。使用原生 label 的背景色、内边距和圆角能力，**不是 backdrop-filter 毛玻璃**；原生透明度与具体位置仍需真机核对。
4. 长店名只在地图名称牌中省略，不修改记录原名。聚合数量与选中店名放在同一名称牌里；未选中的单点不增加名称，避免满地图文字。
5. 分组计算为所选名称牌在图标下方的占位增加余量，坐标不变。
6. **右图改为 280 × 280rpx 正方形**，`aspectFill` 裁切；上、下、右均留 30rpx。卡片总高度仍为 340rpx。
7. 左栏标题调整为 42rpx／54rpx 行高；辅助信息与日期统一为 24rpx／34rpx 行高，统一左边距、行距及字间距，不拉伸文字。
8. 位置操作保持原功能与中文，英文缩短为 Correct／Open map，完整说明保留在无障碍标签中，给内部收起按钮留出空间。

点击照片打开详情、收藏、收起展开、邻近记录选择、原生地图居中与约200ms尺寸过渡保持。

## 验证

193/193 静态检查、128/128 mock 场景通过，比上一版新增 3 个测试：下方名称牌和明暗主题、长名称与聚合数量、方图及内部按钮布局。之前的非方图尺寸断言按用户本次授权改为280rpx，并保留其余检查。

验证包含状态和布局配置，不代表原生 map 的 label 已在真机上验收。不同基础库的背景透明度、字体度量与点击行为可能存在差异；仍需通过用户手机检查下方位置、完整显示与底部操作区域。

## 更新方法

只需用最新包重新编译前端。无云函数变更，无需清缓存、重新上传照片或重建记录。
