# 中文排版、统一玻璃卡片与 Me 紧凑布局

## 本轮范围

依据五张反馈截图微调 best-ui；保留原生页面结构、导航、TabBar、Sheet、主题、语言、地图及云端保存主链路，不进行新的业务大迁移。

- **中文字体**：移除标题字体链中的宋体回退，改为 PingFang SC / Noto Sans CJK SC / Microsoft YaHei / sans-serif；英文和数字仍优先使用已有 SavorSerif。中文首页主标题、页面标题、Add / Sheet 标题和分区标题单独调整字号、字距和字重。小字字号、正文与菜单副标题不变；不新增字体下载。
- **Countries → 收藏回忆 / Saved memories**：Me 统计全部真实书签收藏，Us 只统计共享记录中的真实书签收藏。仅 `saved === true`，不混入 `liked`，排除原始演示记录；同一记录 ID 只计一次，空数据为 0。随 store 更新自动刷新。保留记录与云适配器中的 country 字段，未增加上传或数据库写入。Me 同时移除固定的「2 年」，整理成三列。
- **统一卡片材质**：Home 周报/最近记录、Map 地点/空状态、Us 两张卡片、Me 统计/菜单，共八处使用 `.content-card`。亮色背景 rgba(251,250,247,.62)，暗色 rgba(24,35,30,.76)，模糊统一 20px，同主题的边框及阴影共用令牌。清理页面级覆盖，避免暗色时又回到不同透明度。卡片背后的图片/地图不同，实际明暗仍会自然变化。搜索、浮层、操作控件及照片不强行套用内容卡片材质。
- **Me 更紧凑**：统计卡 360 → 260rpx；装饰图 122 → 60rpx；菜单行 140 → 124rpx；头像区下方间距 58 → 42rpx。未缩小菜单副标题，保持充足点击区域和原有滚动。
- **Home 背景衔接**：背景图片显式补全 100vw / 100vh，避免默认图片尺寸使卡片背后的环境模糊在中段截断。

## 验证

`npm run verify:all`：**188/188 静态检查、53/53 mock 场景通过**，包含原 TabBar、资源、云适配/隔离、语言、地图气泡测试。新增收藏范围/演示排除/去重/响应更新、国家字段保留、八张卡片共享材质、Me 高度和中文字体检查。原 UI baseline 未改；授权变更通过独立 approved hash 记录。

**未执行微信开发者工具、真机、线上 CloudBase 验证。** CSS backdrop-filter 在原生地图层上的实际表现及不同系统中文字体回退，需要设备验收，不能用本地断言代替。

### 微信端验收建议

1. 简体中文查看五页标题及 Sheet：不再回退宋体；小字不变，英文人名和数字仍保留衬线风格。切回 English 检查标题无中文专属字号覆盖。
2. 亮色/暗色分别查看 Home、Map、Us、Me：主要卡片采用同一材质，无页面级暗色覆盖；地图操作、气泡、搜索仍可用。
3. 新增真实记录后收藏：Me +1；仅共享记录使 Us +1。仅点赞不加数；取消收藏恢复；样例收藏不计入。
4. Me 三列数字不挤压标签，图形不遮挡数字，菜单更早出现，底部菜单和二级 Sheet 均可滚动。

## 仍然保留的业务边界

本轮只让替换后的「收藏回忆」成为真实计数。Home / Us / Me 其他餐食、地点指标仍沿用现有演示基数，图表仍是装饰，不能理解为真实统计已全部完成。旧日期/地理元数据/其他表单字段及云编辑同步等审计项未擅自扩展。上轮业务报告和文件扫描作为历史快照保留。

本轮无云函数、数据结构或环境配置变更；若上一版云函数已正确部署，仅需导入最新前端并重新编译。
