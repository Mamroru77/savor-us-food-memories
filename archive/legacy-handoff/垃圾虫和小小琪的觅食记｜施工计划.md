# 垃圾虫和小小琪的觅食记｜施工计划

## 1. 产品定位

一句话：**两个人共同记录的美食生活地图，把一起吃过的每一家店留在地图、时间和照片里。**

产品不是“大众点评情侣版”，也不是纯恋爱相册，而是：

- 50% 美食记录：餐厅、地点、日期、菜系、人均、菜品、评分、是否二刷。
- 50% 情侣回忆：双方评分、双方留言、照片、特殊标签、纪念节点。
- 照片是第一视觉核心。
- 两个人都能共同新增、补充、编辑。

当前正式名称：**垃圾虫和小小琪的觅食记**。

## 2. 视觉方向

### 基础气质

- A 温暖情侣手账感 + C 轻可爱情侣感。
- 奶油白、暖米色、浅杏、低饱和粉橘/珊瑚色。
- 大照片、大留白、圆角卡片、轻阴影。
- 少量手绘心形、星星、餐具、贴纸元素。
- 不做过度粉色，不做儿童化卡通 UI。

### 参考图

重点参考：

1. `references/design/两人食记小程序五屏展示.png`：核心五屏视觉方向。
2. `references/design/a_clean_pastel_app_ui_design_mockup_flow_chart_p.png`：万能导入流程。
3. `references/design/甜蜜餐点主题食谱本.png`：当前头像/品牌视觉方向。

## 3. 核心导航

底部导航目标结构：

- 回忆
- 地图
- 中间 `+` 记一顿
- 想吃
- 我们

### 回忆

首页 + 时间轴：

- 顶部：`垃圾虫和小小琪` + 已一起吃过 N 家。
- 最近一次大图卡片。
- 美食地图预览。
- 吃过/城市/想吃统计。
- 最近的回忆横向照片流。
- 未来：一年前的今天、纪念节点、年度报告。

### 地图

- 微信 `<map>` + 腾讯位置服务。
- 展示所有吃过的餐厅 POI。
- 同一餐厅多次去过只保留一个餐厅实体，但详情中显示 N 次到访。
- 未来筛选：城市、菜系、评分、她喜欢、值得二刷等。

### 记一顿

最核心原则：**低门槛**。

第一版字段：

- 餐厅名称（必填）
- 日期
- 城市
- 地址
- 菜系
- 人均
- 照片（最多 9 张）
- 垃圾虫评分
- 小小琪评分
- 菜品列表
- 一句话回忆

未来接入腾讯 POI 后，餐厅名/地址/经纬度尽量自动带出。

### 想吃

- 两个人共同 Wishlist。
- 从“想吃”到“已吃”的状态迁移。
- 未来支持标签：约会、甜品、火锅、旅行、小红书种草等。

### 我们

- 情侣绑定
- 共同数据统计
- 年度报告
- 账号与权限
- 数据管理/批量导入

## 4. 数据模型原则

必须区分：**餐厅实体** 和 **一次用餐记录**。

示例：

```text
restaurants
└── 鼎泰丰·静安嘉里中心店
    ├── dining_record #1  2026-08-17
    └── dining_record #2  2027-03-21
```

推荐集合：

```text
users
couples
restaurants
dining_records
record_members
wishlist
imports
```

照片可以优先使用云存储 `fileID`，不必单独拆 `photos` 集合，除非后续需要复杂照片元数据。

### dining_records 最低字段建议

```js
{
  _id,
  coupleId,
  restaurantId,       // 后续 POI 归一后加入
  restaurantName,
  date,
  city,
  address,
  location: { lat, lng },
  cuisine,
  perCapita,
  ratings: {
    lajiChong,
    xiaoXiaoQi
  },
  dishes: [],
  note,
  photos: [],
  memberOpenids: [],
  createdBy,
  createdAt,
  updatedAt
}
```

## 5. 技术架构

### 已确定

- 微信原生小程序。
- 微信云开发 CloudBase：云函数、数据库、云存储。
- 腾讯位置服务 + 微信 `<map>`。

### 当前云环境

`miniprogram/app.js` 中已配置 `wx.cloud.init` 和实际环境 ID。

### 地图策略

- 不把大众点评/美团 ID 当内部主键。
- 第三方导入最终全部归一到内部 `restaurant_id`。
- 腾讯 POI 负责标准名称、地址、经纬度和地图落点。

## 6. 分阶段施工

### Stage 0｜注册与备案（已完成）

- 微信小程序注册。
- 类目：工具 → 备忘录。
- 小程序备案已成功。
- 云开发免费环境已创建。

### Stage 1｜静态首页（已完成）

- 首页视觉骨架。
- 回忆/地图/统计/空状态/底部导航。
- 当前已能出现页面。

### Stage 2｜第一条真实数据链路（进行中）

目标：

```text
记一顿
→ 上传照片到云存储
→ mealRecords 云函数
→ dining_records
→ 返回首页
→ 首页刷新最新记录和统计
```

已有：

- `pages/add/`
- `cloudfunctions/mealRecords/`
- 首页 `loadRecords()`
- 图片临时 URL 转换

当前阻塞：`pages/add/index.wxml` WXML 编译错误，详见 `notes/BUG_HANDOFF.md`。

验收：

- [ ] 首页无编译错误。
- [ ] `+` 能打开“记一顿”。
- [ ] 两人评分可操作。
- [ ] 可添加/删除菜品。
- [ ] 可选照片。
- [ ] 部署 `mealRecords` 后能保存。
- [ ] 保存后返回首页显示最新餐厅。
- [ ] 首页餐厅数/城市数正确更新。

### Stage 3｜情侣绑定

优先于继续堆页面。

流程：

```text
垃圾虫登录
→ 创建 couple
→ 生成邀请码/二维码
→ 小小琪接受
→ 两个 openid 绑定同一个 coupleId
→ 两个人共享 dining_records / wishlist
```

要求：

- 数据访问不再仅按创建者 openid，而是按 coupleId 授权。
- 两个人都可补照片、评分和编辑。
- 关键操作记录 `updatedBy`。

### Stage 4｜餐厅实体 + 腾讯地图

- 新建 `restaurants` 集合。
- 接腾讯位置服务 POI 搜索。
- 新增记录时选择餐厅位置。
- 做餐厅去重。
- 地图真实显示吃过的店。
- 餐厅详情支持多次到访。

### Stage 5｜想吃清单

- Wishlist 新增/删除/标签。
- “想吃 → 已吃”迁移。
- 可从想吃条目直接发起“记一顿”。

### Stage 6｜餐厅详情与时间轴

- 餐厅详情：双方评分、菜品、照片、回忆、到访历史。
- 时间轴：按日期浏览全部回忆。
- 标签：生日、旅行、纪念日、踩雷、值得二刷、她超喜欢等。

### Stage 7｜万能导入

优先官方合规和稳定性，不依赖平台账号历史订单 API。

第一版：

- 粘贴美团/大众点评/淘宝闪购/京东外卖分享链接。
- 解析店名/地址等公开分享信息。
- 腾讯 POI 匹配同一家店。
- 可加入想吃或记录去过。

第二版：

- 截图 OCR：订单/店铺截图识别。
- 提取店名、日期、金额、菜品。
- 用户确认后导入。

### Stage 8｜长期体验

- 一年前的今天。
- 第 100 家餐厅。
- 年度美食报告。
- 城市/菜系图鉴。
- 双方口味差异趣味统计。
- 随机去哪吃。

## 7. 安全与权限

- 客户端不能拥有可绕过 couple 权限的裸数据库写权限。
- 写操作优先走云函数校验 openid/coupleId。
- 图片 fileID 与记录归属保持一致。
- 删除操作后续要有二次确认。
- 第三方导入不要抓取需要绕过登录/风控的数据。

## 8. 当前最优先任务

**不要继续加新功能，先把 Stage 2 编译和数据闭环跑通。**

顺序：

1. 修复 `pages/add/index.wxml`。
2. 微信开发者工具全量编译。
3. 部署 `mealRecords` 云函数。
4. 新增一条无照片测试记录。
5. 验证首页读取。
6. 再测试照片上传。
7. 通过后进入情侣绑定。
