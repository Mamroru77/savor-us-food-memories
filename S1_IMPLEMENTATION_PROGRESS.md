# S1 身份与缓存分区：合并与实现说明

更新：2026-09-14。**候选代码已接入运行时并通过本环境自动验证；未部署、未推送、未完成开发者工具/真机验收，不是已上线功能。**

## 1. 合并基线

- 从 `fix-9.13@6b92af1` 快进合并用户最新提交 `4daeb2e`（Restore production UI and align map card toggle）。
- 保留正式 UI、K1/VP1 的移除和 Map 卡片开关样式；Tab 抽动经用户同意暂缓，不再作为 S1 阻塞。
- 前一批新增的 account、identityPartitions 和测试已整合，不是另起一套身份实现。
- 原工作区增量已备份；没有覆盖 Windows 本机工程或推送远端。

## 2. 本轮范围与实现

| 范围 | 实现 |
|---|---|
| 服务端可信身份 | `account.bootstrap` 仅从 `getWXContext()` 获取 APPID/OPENID；内部映射键为服务端散列，产品 ID 为随机值。并发首次请求回读胜者，不覆盖映射。 |
| 最小协议 | 返回本人产品 ID、资料/成员版本和服务端时间；没有共享关系或公开资料查询。 |
| 跨身份云请求 | `mealRecords` 要求协议版本及 expectedUserId，并重新通过真实服务端身份查映射。查询、新建、修改、删除和位置接口均受保护。 |
| 版本错配 | 会话激活前检查 mealRecords 身份握手；每次记录写操作前再次握手。旧服务端不能忽略新字段后接收写入。 |
| 身份状态 | unknown/verifying/verified/offline-known/blocked；冷启动和前台重新验证期间锁定敏感内容。已验证的同一运行会话离线仍可保存本机内容。 |
| 本机分区 | 新增 `savor-identity-partitions-v1` 单一版本化信封，按产品 ID 分区；store 的 diary/draft 逻辑入口经中央 identity adapter 读写。 |
| 旧数据保护 | 原 `savor-diary-v1`、`savor-draft-v1` 物理键不删除、不覆盖；首次成功迁移复制原始值到 legacyQuarantine，写失败不激活身份。 |
| 草稿/outbox | 新草稿和操作携带 actorUserId；不匹配时拒绝持久化/发送。未知旧队列不自动贴当前身份、不自动迁移 flags。 |
| 异步保护 | store 的同步、队列、创建、编辑回执和位置操作检查原会话租约；A 的迟到结果不能提交到 B。 |
| 页面/媒体 | 切换身份时清理页面私人状态、Sheet、Map 图标渲染缓存；临时 URL/下载检查租约。新本机照片目录按用户分区。 |
| 原生选择器 | 照片/位置等原生选择器返回后，可在重新验证为**同一用户**时恢复；这项能力不用于云响应或旧队列写回。 |
| 恢复入口 | Me → 账号与旧数据：只读核验旧操作、导出原始隔离备份、导出当前完整私人分区、明确复制旧记录/文字草稿/昵称简介。 |
| 导入边界 | JSON 日记导入生成本机私人副本，去掉云 ID/权限/共享关系/历史操作与私有照片引用，来源 ID 去重；不上传。 |
| 资料与偏好 | 昵称/简介/头像仍是本人分区的本机能力，不静默覆盖云资料。主题/语言/减少动效为设备偏好；其余偏好按账号。 |
| UI | 五 Tab 保持；新增的是非 Tab 的 account 页面。身份锁定与恢复页支持简中/英文，不翻译用户内容。 |

### 重要体验与安全取舍

1. 首次升级看到空的当前私人分区是预期行为，不表示删除旧数据。原数据在隔离区；本人云记录可由 owner-scoped 同步恢复。
2. 冷启动离线或前台重新验证失败，默认锁定敏感旧分区。不能把本机记住的产品 ID 当作可信登录。联网验证后恢复。这里选择安全，不承诺所有离线重启都能直接查看日记。
3. 明确复制旧记录只生成本机私人内容，不自动重放未知写入。`confirmed` 仅表示 owner-scoped 结果查询确认；`unresolved` 不表示可重建或可删除。
4. 当前完整私人分区备份含草稿与 outbox，是保全/排障文件；**不提供自动导入并重放该文件内操作的入口**。普通日记导入仍只导入本机副本。
5. 本机照片自动删除/GC 暂时采取“保留”策略：不能因只扫描当前账号而删除其他账号或隔离区引用的文件。云文件同样不删除。代价是本机磁盘使用可能增长；后续有完整跨分区引用登记后再开放清理。
6. 用户主动分享的备份已离开应用保护边界，不自动抹除；导出前明确提示敏感性。
7. 资料版本/成员版本目前为零值基础契约；不意味着云资料同步、双人绑定、伴侣照片授权已实现。这些不属于本轮范围。
8. 新建 account 集合或云函数未部署时显示验证失败/锁定是预期安全行为，不能删除门控让页面“先跑起来”。

## 3. 验证结果

本环境实际执行：

```
npm run verify:all
```

包含全部既有回归及 `verify:identity`。最新结果以交付的 `S1_TEST_RESULTS.txt` 为准：
- 静态检查通过；Tab 21/21、业务 mock 233/233、handoff 74/74、UI 44/44、采集自检 4+2 通过。
- 身份基础测试通过；真实 identity/store/service 模块、mock 云传输的 S1 运行时对抗场景 22/22 通过。
- 覆盖 A/B 隔离、冷启动锁、未归属旧请求、离线队列、丢响应、迟到 list、上传中换号、错误协议、服务端身份不匹配、配额、损坏版本、原生结果同账号恢复。

历史业务测试使用明确的“已知验证会话” fixture 来隔离业务回归；**它们不是身份隔离证据**。身份隔离证据来自独立的 `verify-identity-runtime.cjs`，该脚本使用真实客户端模块及真实 mealRecords handler，不使用那个 fixture。

测试变更没有删除 Tab/业务用例；更新了“自动种入七条 sample”“旧 flags 自动认领”等与新契约冲突的预期。样式 hash 变更仅对应 S1 页面门控/入口和异步保护；不等于原生 UI 验收通过。

**未执行：** 微信编译、模拟器实际布局、真实 CloudBase 部署与权限、两个真实微信账号/设备、杀进程/键盘/选择器时序、VoiceOver/TalkBack。不得把 Node mock 通过写成这些项目通过。

## 4. 后续执行

先给本地 Codex 使用 `CODEX_S1_TEST_PROMPT.md`。经本地检查与授权部署后，再按 `S1_DEVICE_ACCEPTANCE.csv` 真机测试。Tab 微抽动不在本次验收门槛内，但导航和你最新 Map 样式不得回归。

## S1-R2 补充审查

本批修复失效验证请求阻塞重试、陈旧 finalizer 清除新请求、设备偏好读入私有字段、错误隔离区结构接受等边界。新增4个运行时反例，当前22/22通过。详见 PROJECT_PROGRESS.md。Codex、真实云端、真机门槛仍待执行。
