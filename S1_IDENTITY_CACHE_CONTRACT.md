# S1 准备稿：身份、缓存与媒体授权契约

**状态：设计准备完成，尚未实现/部署以下新接口。** 本稿承接“独立账号 + 主动共享”的已确认方向，不代表可以开放跨账号访问。

## 1. 当前事实与阻断项

- 当前 `mealRecords` 通过服务端 OPENID 获取身份，餐食读写仍限制 `createdBy`。
- 当前客户端 `savor-diary-v1` 存放一份日记状态，没有应用层的已验证账号分区或成员版本。
- 尚无用户 bootstrap、云端公开资料、邀请码、情侣成员关系或伴侣照片授权。
- 当前编辑草稿的操作结果恢复不能直接当作未来跨账号迁移协议；新身份域必须有可验证的确认/隔离规则。
- 不能仅凭源代码断言微信平台实际会跨账号共享本机存储；其平台隔离表现需要 S0 真机验证。也不能反过来省略应用层身份校验。

## 2. 服务端身份基础

建议新增统一账号初始化能力，暂称 `account.bootstrap`：

- 输入：客户端协议版本，不接受可用作授权依据的 OPENID/userId。
- 服务端：通过 `getWXContext()` 获取当前身份，生成或查找稳定的产品用户 ID。
- 输出：产品用户 ID、最小资料版本、当前成员关系版本、服务端时间和支持的协议能力。
- 输出不提供其他用户的私人资料，不把业务凭证写入日志。
- 客户端提交的 userId 只能作为缓存上下文提示；每个写操作仍重新校验服务端身份。

建议采用服务端保存的随机产品用户 ID 映射，不把可逆身份信息放入邀请码，也不依赖客户端自算 ID 进行授权。

## 3. 最小云资料

第一版仅需要昵称、头像资产引用、资料 revision、创建/更新时间。

- 首次同步先展示本机资料并确认，不静默覆盖已有云资料。
- 昵称与头像是本人维护的信息，不要求真实姓名或手机号码。
- 语言、主题、减少动效继续作为设备偏好；完整偏好/个性化同步另做选择与冲突设计。
- “可供伴侣看到的资料”是有限投影，不等同向所有账号公开整份 users 文档。

## 4. 保留旧存储键，升级内部结构

保留 `savor-diary-v1` / `savor-draft-v1` 兼容入口，建议内部加入：

- `formatVersion`：缓存结构版本；
- `verifiedUserId`：由成功 bootstrap 确认的产品身份；
- 本人私有记录分区；
- 按用户 + 情侣空间 + 成员版本组织的共享缓存；
- 带用户/空间上下文的 outbox；
- `legacyQuarantine`：尚未能安全归属的旧缓存/草稿/操作。

不一次性擦除旧键，不在迁移失败时重置空日记。迁移必须先生成备份或可恢复副本，再原子提交新结构。

## 5. 冷启动与离线策略

建议状态机：

`unknown → verifying → verified(userId) → offline-known / identity-changed / blocked`

- 身份未确认时，不自动提交历史 outbox。
- 明确发现身份变化时，隔离旧队列及旧私有缓存，不以新账号重放。
- 同一已确认会话离线时保留本人草稿与操作。
- 冷启动离线、平台无法提供可信身份线索时，不能同时承诺“旧缓存始终可见”和“账号切换绝不串数据”。建议默认锁定敏感旧分区，保留数据等待验证；最终体验策略需结合 S0 平台行为确认。
- 共享缓存独立设置授权有效期；到期需联网复核成员关系。私人草稿不能因为共享授权过期而被删除。

## 6. 旧 outbox 的安全迁移

旧操作没有已验证 actor 标记，不能把它们全部贴上当前登录者身份。

### 已有云记录的修改

逐条通过当前账号的 owner-scoped 查询验证记录归属，再决定是否绑定到当前分区。查不到不等于可以重建或删除，先进入待处理区。

### 待确认的新建请求

旧请求的主键与原服务端身份相关。建议增加 owner-scoped 的新建请求结果查询能力：

- 当前身份可证明已经创建：恢复记录及确认状态。
- 当前身份下未找到：不能分辨“原账号尚未提交”和“这是另一个账号的请求”，不得自动重放。
- 提供恢复原账号、导出草稿或明确复制成新的本人私人草稿等路径；复制时使用新请求身份并保留原未知操作，不静默丢弃。

### 编辑完成确认

未来采用持久化确认凭据/服务端操作查询，不把“队列里没有这个 ID”单独视为成功证明。确认凭据还需绑定用户、记录与操作类型，避免跨身份迁移误判。

## 7. 操作上下文与权限

建议操作信封包含 operationId、目标 ID、baseRevision、用户缓存上下文、可选 coupleId/membershipVersion 和 typed payload。

服务端按真实身份重新验证：

- 是否有权访问目标；
- 是否属于当前活跃空间；
- 成员版本是否匹配；
- 哪些字段可由当前用户修改；
- 操作是否已执行、实际写入版本是多少。

S0 新增的实际写入版本回执是基础，但它不是用户身份或情侣授权令牌。

## 8. 媒体授权准备

建议接口链路为 `prepareUpload → 客户端上传 → finalizeUpload → 授权读取`。

- 服务端分配绑定发起者的上传会话与目标路径。
- finalize 验证对象、归属、大小/类型及关联业务对象，不能仅信任客户端给出的 fileID。
- 查看伴侣图片前校验活跃成员关系、餐食共享状态和资产引用。
- 保持私人存储规则；不把所有云图片公开读取。
- 旧文件只有在归属可被验证时才纳入共享资产库。平台若不支持所需的归属验证，使用重新上传等安全迁移，不以管理员权限任意代签。
- 具体对象元数据校验能力需在实际 CloudBase 环境验证，本稿不把它标为已经具备。

## 9. S1 进入实现前的验收契约

- A 的缓存/队列不能在 B 的身份下自动发送。
- 账号变化不会删除 A 的未同步内容。
- 未验证的旧新建请求不能生成 B 的新餐食。
- 本机资料不会静默覆盖云资料。
- 客户端伪造 userId、成员列表、资产 ID 均不能获得权限。
- 未知身份/缺失回执/版本不兼容时保守阻断，提供可恢复路径。
- 即使成功 bootstrap，也不能自动把历史 `shared` 标记发布给新伴侣。

## 10. 本轮不做的事

不创建 users/couples/assets 集合，不部署 bootstrap，不更改当前数据库/存储权限，不声称客户端缓存已经完成按账号分区，不新增任何真实跨账号共享入口。

## 后续实施注记（2026-09-11）

SHARE_IMPORT_UPDATE.md 所述补丁已实现 owner-scoped 操作回执核验：编辑不再仅凭本地 outbox 缺席认定完成。回执有界，未实现旧／淘汰回执的完整恢复协议，也不等于本契约的 bootstrap、身份缓存分区或媒体授权已经落地。
