# Stage 2 + best-ui：最短部署步骤

> **最新业务更新**：当前以 [BUSINESS_1_TO_5_UPDATE.md](BUSINESS_1_TO_5_UPDATE.md) 为准。本轮新增云端编辑、删除与状态同步，必须重新部署 mealRecords，并同步升级前端。


> 可部署候选版。尚未在微信开发者工具、真机或实际 CloudBase 验证；本地通过不等于最终验收。

## 正常部署：六步

1. 微信开发者工具导入 **`savor-mp/`**（下载压缩包中同名目录）。新增根 `project.config.json` 指向原 `miniprogram/`，没有搬动页面。原有 `savor-mp/miniprogram/` 入口仍保留；完整云函数部署优先使用父目录工程。
2. 使用原真实 AppID `wx65867b5568b82996` 登录具有权限的开发者账号；确认云环境仍为 **`cloud1-d9gqm52id66c0bcda`**，不要换成测试号或其他环境。
3. 右键 `cloudfunctions/mealRecords` → **上传并部署：云端安装依赖**，选择上述环境。必须部署本次版本，不只是旧 integrated 版本。云端保持 `DYNAMIC_CURRENT_ENV`。
4. 点击编译。首次 Home list 会由云函数创建 `dining_records`（已有集合不删除、不清空、不迁移旧记录）。
5. **首次部署需确认一次安全规则**：数据库 `dining_records` 客户端读写均禁止，例如自定义规则 `{"read":false,"write":false}`；云函数通过服务端 SDK 执行授权读写。云存储使用“仅创建者可读写”或等价的已验证权限规则。若已有更严格安全配置，不放宽；不要设置所有用户可写。客户端代码不能安全修改环境权限，因此这里需要控制台确认。
6. Add 填写 → 选图 → 保存 → Home 查记录与照片；重新启动确认恢复。再测无照片、单图、多图和断网恢复。保存失败时 Draft 应保留，再点 Save 安全重试。

无需手动创建集合、导入 7 条 sample、配置情侣绑定，也无需在本地安装前端框架。若集合创建失败，界面继续使用缓存，但需检查云函数环境/服务权限；代码不会吞掉权限错误假装成功。

## 保存失败后的操作

- `Could not reach mealRecords...`：先确认函数已部署到正确环境，再检查网络。
- 部分图片上传失败：Draft 保存已上传文件的 fileID，重试会复用；尚未提交任何不完整记录。
- 请求已发出但结果未知：Draft 带有固定 request ID 与保存快照。请先重试 Save 确认上一顿的结果，再修改表单，以免一次请求变成两顿饭。关闭/切 Tab 不丢快照，不必清缓存。
- 本地存储满：先释放空间；不能持久化幂等 ID 时不会发起云写入，当前表单仍保留。
- 上传过程中离开页面：操作可继续完成，成功后更新 store，但不把用户强行导航回 Home。下一次 Home 会同步。
- 如控制台报告索引缺失，按实际错误给出的索引建立提示处理；这里未连接真实环境，不能预先声称索引已验证。

## 本阶段的边界（避免误解）

- 新建真实餐食与照片是云持久化数据；演示数据和 Import 仍只在本地，不自动上传。
- 云记录的收藏/喜欢/共享标记已接入持久化队列和 owner-scoped 云同步；情侣绑定和向其他用户共享未启用。
- 云记录删除已改为 tombstone 同步，不会被新客户端重新同步复活；云照片不立即物理删除。历史本机隐藏不自动转为云删除。
- 旧未知位置进入待确认流程；新 Add 使用腾讯原生选点与本次地址元数据，不使用固定城市中心。
- 旧双人评分均值只用于兼容显示；未评分使用 0，且不写成单评分。编辑其他字段不会把旧均分转换为新投票。
- 图像持久化为 `cloud://` fileID，原生 image 负责读取；临时 URL 工具仅按需生成，不写进日记。离线保证的是本地记录/输入可用，云照片并不保证离线可见（Memory Row 有打包图回退）。
- 部分上传后主动编辑/放弃 Draft 或清空设备数据，可能遗留未关联云文件。本轮优先不误删已经提交但响应丢失的照片，未加入跨用户扫描/自动垃圾回收。正常重试复用已上传文件。
- Home/Us/Me 及周报/旅程已使用真实日记统计，排除样例。地点归并口径和城市确认规则见本轮说明。

## 本地验证

在 `savor-mp/` 下执行：

```bash
npm run verify:all
```

不需要 npm install（验证脚本均为 Node 内置依赖；云函数 SDK 由云端安装）。

包括原全部静态验证、TabBar、资产，以及 32 个 mock 契约场景。详细日志见 `reports/`。请按照上述真实设备闭环另行验收。
