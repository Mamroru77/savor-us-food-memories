# 地图选中居中修复与标记候选

## 已修复的代码问题

旧 applyFilters 每次选中餐厅时，先把中心设为该餐厅，然后无条件调用 includePoints 展示全部地点，导致最终视野偏离选中餐厅。

本轮将相机行为分开：
- 点击标记／主动搜索筛选：更新卡片后调用 moveToLocation，目标是所选门店的真实坐标。再次点击同一标记也会重新居中。
- 点击原有全览／回正按钮：使用 includePoints 展示全部地点。
- 普通缓存／收藏状态刷新：同一门店坐标未变时不抢走用户正在拖动的视野。
- 选中门店位置被校正，或筛选改变所选门店：重新定位到新的有效坐标。

这里的居中指原生 map 组件的中心；未改变门店坐标，也未改 Me 高度、浮层尺寸或地图底图。实际手机上的地图动画和遮挡情况仍需验收。

## 用户自制「地标快印」已应用

用户最终提供自己的照片＋S 字标＋叉勺组合图并要求应用，不再使用早前候选。

- 参考：`design-references/landmark-user-reference.png`。
- 最终预览：`design-references/landmark-stamp-preview.png`。
- 当前实现、渲染边界与部署说明见 `LANDMARK_STAMP_UPDATE.md`。

## 上轮居中修复的验证和部署

本次标记实施与最新验证见 LANDMARK_STAMP_UPDATE.md。

191/191 静态检查、110/110 mock 场景通过；新增标记点击不全览、同点重新居中、被动刷新保持视野、显式全览及空结果测试。压缩包解压后复验通过。

仅客户端地图逻辑改变，已经部署过 6440961 配套 mealRecords 的环境无需为本次地图修复再次部署云函数，重新编译前端即可。

用户此前反馈：重新部署 mealRecords 后同步恢复。该反馈不等同于全部双设备并发、安全规则或其他真机验收已完成。
