> 历史说明：本文为94585cb旧机制，其全实例准备/800ms等待已撤销。当前实现及未闭环事项见 `TAB_NAVIGATION_INDEPENDENCE_FIX.md`。

# Tab 原生回跳修正版：原子状态与视图交接

日期：2026-09-13；基线：bb2cb52（保留该版全部 Map 调整）。

**状态：源码及延迟提交模型验证通过，修正版真机验收未执行。不是已确认修复。**

## 原始视频证据

文件：[bug-tab-switch.mp4](https://github.com/Mamroru77/savor-us-food-memories/blob/integrated-with-cloud/bug-tab-switch.mp4)

4435500 字节，1080×2374，时长约6.413秒，解码334帧；SHA256：`a54a520451d08983514109ffa328119b2d26faeb867ace1a76405e6e770e7332`。

使用原始帧时间戳，而非假定60fps换算：

| 时间 | 已观察画面 |
|---|---|
| 2.9843s | Us选中，Add已经显示竖直餐具 |
| 3.0915s | Add回跳成交叉餐具，随后才退选形变 |
| 约3.759s | Add再次到达竖直餐具端点 |
| 4.2625s | Me选中 |
| 4.3276s | 短暂重新显示Us选中、Add交叉餐具的旧组合 |
| 4.3604s | Me恢复选中 |

视频证实了显示顺序异常，但不能单凭画面确定微信原生合成器内部原因，也不能确定录制包的源码提交。以下修复针对现有源码中允许此类旧状态覆盖的路径，不将模型测试描述为原生根因证明。

## 修复内容

### 1. 图标改为接收单对象命令

Tab不再分别传递name/fromName/entryKey/entryActive/color/quiet等响应式属性。每个图标接收一份含起点、终点、访问key、递增revision、播放状态、配色及480ms的完整命令。

子组件将接受的命令及内部状态一次性提交。迟到的旧命令不能覆盖较新命令；绘制层必须匹配当前revision。TabBar的选中文字/ARIA状态也读取同一个发布快照，而不再直接跟随其他setData中的selected。

### 2. 静态端点不再经过第二层s-icon更新

Tab专用备用图像由官方`iconSvg`直接生成，跟随子组件状态更新，不再通过嵌套s-icon的name/color观察器重新生成src。通用s-icon及其他页面的图标不改。

备用image按不可变SVG URI建立节点身份。旧图像加载事件只有在对应URI与当前需要的端点完全一致时才能放行，既阻止错误旧图放行，也避免相同URI没有再次触发load造成等待。

新建子组件首先接受完整命令再绘制，不主动挂载默认Home图案。这里仍须真机检查首访是否出现可见空白；不能用“没有错误图案”替代这项验收。

### 3. 交接等待父、子视图提交及静态图像加载

原方案在循环发出setData后立即switchTab；这不等于各个缓存TabBar的子视图已准备好。

新方案准备已挂载实例，等待父组件提交、五个图标各自提交以及所需静态端点load后才导航。目标页明确showSelection后才播放。

父组件回调晚到时会复查发布revision与当前路由；子组件在已隐藏之后收到旧load，也不能重新激活旧动画。相同访问重复通知不重新起播。

单个图标的静态图像load/error若1200ms仍未返回，则报告svg-static-timeout并降级为目标端点，避免一直卡在起点。

若原生视图不返回确认，800ms上限保证导航不会永久阻塞，并记录prepare-timeout。**800ms不是每次固定等待，也不是动画时长。超时路径不保证缓存位图已准备好，若真机触发必须继续定位，不能作为通过验收。**

### 4. 五个页面统一走有路由校验的入口

五个onShow均调用showSelection，不再在setData回调里无条件replay。交接期间拒绝源页的旧选中状态；非交接期间要求页面声明的选中项与实际路由相符。

i18n只更新白名单中的主题、Quiet和文案，不能夹带selected或entry字段，也不能由旧页的异步刷新覆盖当前页外观。

导航失败恢复源页；旧失败回调不能回滚后续访问。程序式Save→Home仍支持，TabBar仍无业务/store导入。

## 保持不变

- Home House↔HouseHeart、Map Map↔MapPinned、Add Utensils↔UtensilsCrossed、Us Users↔UsersRound、Me User↔UserRound。
- 五Tab 480ms、真实几何双向形变、32ms逐步推进、原有首帧load/提交确认、Quiet与错误降级。
- Map堆叠及底部卡片320ms、原生图层、按钮、照片、坐标及bb2cb52修复。Map文件只更换onShow的Tab同步入口。
- Tab尺寸、主题、业务/API、云权限、数据存储、照片清理、其他页面布局。

480ms仍是形变帧预算。图像准备时间、视图提交耗时可能增加用户感受到的总时间；不以配置数值或mock证明真机观感不变。

## 验证

`npm run verify:all`：
- 229/229 静态
- 21/21 Tab结构/页面接入
- 233/233 原有mock契约
- **26/26 新增延迟提交/跨页交接回归**
- 42/42 UI回归
- 48官方SVG、28别名、73模板图标声明

新增测试显式延迟父/子setData确认、静态图像load和动画首帧确认，覆盖视频中的Add→Us→Me、旧回调晚到、相同URI、隐藏后回调、五组端点的480ms去重与反向、Quiet、错误、失败回滚、超时及程序式导航。原有断言按新的接入方法更新，没有删除业务或Map覆盖。

这些模型不模拟微信原生合成器；实际ZIP解压副本也须完整验证后再交付。

## 真机复测

1. 加载此修正版后，按原视频：Home→Map→Add→Us→Me。
2. 不退出小程序，再做Me→Us→Add→Us→Me，至少三轮，必须覆盖缓存页面回访。
3. 观察Add是否出现竖直→交叉→竖直，以及Me选中后是否短暂回到Us选中。也检查是否新增空白帧、无关Tab变形、点击等待或速度变化。
4. 在形变中途切回上一个Tab；再检查浅/暗主题和Quiet。
5. 若复现，请提供修正版原始MP4、手机型号、系统和微信版本，以及运行方式（真机调试/预览/体验版）。不要只以清缓存后首访作为通过。

## 有界本地诊断

TabBar保留最近96条UI交接事件，仅内存、不上传、不持久化、不记录业务内容，也不逐帧轮询。含实例编号、选中索引、访问key、发布revision、prepareMs、prepare-timeout，以及现有SVG报告的实际形变/首帧等待耗时。

在支持小程序逻辑层调试的控制台可读取：

```js
JSON.stringify(getCurrentPages().slice(-1)[0].getTabBar().getTransitionTrace())
```

若仍失败，该记录可帮助区分旧回调、图像等待和原生显示；不是原生绘制完成的证明。
