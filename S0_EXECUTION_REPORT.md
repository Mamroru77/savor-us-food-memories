# S0 执行记录：第一批本地加固

基线：`75c11d3`。用户选择并行推进本地复核与 S1 准备；已于 2026-09-11 收到 3 条点评、2 条美团外卖样例；首轮网页探测见 [S0_LINK_FINDINGS.md](S0_LINK_FINDINGS.md)。

**状态：本批次本地加固已完成；S0 整体尚未通过上线门槛。** 真机、真实 CloudBase、安全规则及链接 App/真机支持仍待验收，不能把本地测试当成完成。

> 后续已实施分享文案导入，并修复缺失编辑队列的回执确认。最新验证为 191/108，见 [SHARE_IMPORT_UPDATE.md](SHARE_IMPORT_UPDATE.md)。下文第一批修复记录保留原验证数量。

## 1. 实际执行的工作

1. 复跑现有验证：190/190 静态检查、85/85 mock 场景通过。
2. 增加“丢响应 + 另一设备修改 + 本机后续完整编辑”的组合回归。
3. 在未修复代码上复现失败：期望保留 `Other device note`，实际被覆盖成 `Local queued note`。证据保存在 `reports/s0-replay-regression-before.log`，全部是合成测试数据。
4. 修复版本推进、编辑快照投影、响应身份校验及草稿完成边界。
5. 全量复验：190/190 静态检查、92/92 mock 场景通过。
6. 产出可填写的设备验收清单、链接支持矩阵，以及 S1 身份/缓存契约草案。

本轮未新增情侣绑定、跨账号共享、链接抓取、Wishlist 或用户资料云同步接口。

## 2. 发现并修复的同步风险

### A. 重试可能覆盖另一设备的修改

原队列在收到某操作的成功回复后，把该记录的后续操作版本推进到“当前云记录版本”。但成功回复可能是旧操作的重放，而当前记录已经被另一设备改过。

复现顺序：

1. 本机排队收藏及完整编辑；
2. 收藏在服务端成功，回复丢失；
3. 同账号另一设备修改备注；
4. 本机重试收藏，收到最新记录；
5. 原逻辑直接提高后续编辑的版本，使旧备注覆盖新备注。

修复：

- 服务端在 `operationReceipts` 中保存最近操作的 `{id, revision}` 回执，响应通过 `operationId`、`operationRevision` 区分本操作与记录当前 `revision`。
- 客户端仅在“确证本操作产生下一版本，且此后无其他写入、后续操作基于同一旧版本”时推进版本。
- 如果回执之后发生了其他写入，后续旧编辑必须产生冲突，不自动覆盖。
- 老函数或旧操作缺少回执时保守处理，不凭当前版本推测可安全推进。可能需要用户处理一次冲突，这是避免误覆盖的取舍。

### B. 待同步编辑快照回滚本机已确认状态

完整 Memory 快照可能包含较旧的 revision、收藏/点赞等状态。现在仅投影可编辑内容字段，不再整对象覆盖身份、版本和个人标记。

### C. 响应与请求不匹配

修改响应必须对应请求中的记录；若提供操作 ID，也必须匹配。`getRecord` 同样校验返回记录 ID，避免异常结果被当作另一个记录的确认。

### D. 旧任务不能清空后来替换的草稿

新增/编辑完成只清理对应请求身份的草稿。不再无条件清空当前草稿；Add 保存过程中不允许通过另一条编辑覆盖其重试草稿。页面卸载后的编辑重试成功也不再调用已销毁页面的 `setData`。

## 3. 新增的 7 项回归

- 丢响应后另一设备插入修改，后续完整编辑不得自动越过冲突。
- 操作实际写入版本与当前记录版本分离，伪造回执字段不被接纳。
- 编辑投影不回滚已确认收藏与 revision。
- 旧操作没有回执时停止自动版本推进。
- 错误记录/操作身份不能确认另一条记录。
- 旧编辑完成不能清除替换后的草稿。
- 正在保存的 Add 请求不能被新编辑覆盖。

原有 85 项测试保留，没有通过禁用旧测试来获得通过结果。

## 4. 部署与兼容

本轮修改了 `cloudfunctions/mealRecords/index.js` 和客户端服务/队列逻辑。使用最新完整包后，需要重新部署 **mealRecords → 上传并部署：云端安装依赖**，再编译前端。

- 不修改现有 AppID 和 CloudBase 环境。
- 不清数据库、缓存、草稿或待同步队列。
- 不更改 owner-only 权限，不开放真实跨账号共享。
- 缺少旧操作回执时，后续操作可能转为显式冲突；先核对云端数据、保留必要的本机文案，再决定是否以云端为准。
- 本轮没有真机验证，也没有代替用户部署线上云函数。

## 5. S0 当前状态

| 工作 | 状态 |
|---|---|
| 现有自动验证复跑 | 已完成，本地通过 |
| 组合故障回归与上述修复 | 已完成，本地通过 |
| 最新包解压复验 | 交付打包时执行，结果见最终验证日志 |
| 两账号/两设备权限验收 | 待执行 |
| 真实 CloudBase 写入/并发/安全规则 | 待执行 |
| 真机九图、选点、键盘、Sheet、跨日 | 待执行 |
| 点评/美团真实分享样例 | 已收到 5 条；文案解析回归通过 |
| 平台链接访问与字段支持结论 | 已执行首轮网页探测，未获得详情；不影响本地文案候选 |
| S1 身份与缓存方案 | 契约草案已准备，未接入运行时 |

设备验收模板：`S0_DEVICE_ACCEPTANCE.csv`。
链接验证模板：`S0_LINK_SUPPORT_MATRIX.csv`。
S1 准备：`S1_IDENTITY_CACHE_CONTRACT.md`。

## 6. 下一步输入

已收到 5 条样例，并按用户确认收敛为点评堂食／美团及其他平台外卖。无需再补齐点评短链、美团堂食或团购商品页作为首批门槛。不要提交订单、付款页面、Cookie、登录凭证或个人账户信息。

有开发者工具/云环境操作权限的人按设备清单验收，并回填“实际结果”和脱敏证据。没有真实执行的条目统一保持“待验证”。
