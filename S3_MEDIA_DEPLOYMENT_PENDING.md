# 共享媒体：最终集中部署与验证清单（未执行）

## 待授权部署

- media 云函数（wx-server-sdk ~2.4.0），以及本批联动更新的 spaces 云函数。
- savor_media_assets 集合，客户端直接读写均禁止；只能经受保护云函数访问。
- 保留原私人存储规则。不得为了共享照片将云存储设置为公开读取或全员可写。
- 共享副本路径为 shared-private/{注册资产ID}/{内容摘要}.{jpg|png}。必须验证退出后的非创建者无法绕过服务直接读取该路径，即使其知道/猜到fileID。
- 不改变现有私人照片路径、数据库数据或腾讯配置。

## 回收配置：默认不启用

只有用户单独批准后才设置：
- MEDIA_ADMIN_OPENIDS：实际管理员账号OPENID的逗号列表；不可从请求event推导管理员。
- MEDIA_RETENTION_DAYS：明确批准的整数保留天数，代码允许1–3650，不存在默认保留期批准。
- MEDIA_PURGE_ENABLED：只有精确值true才允许物理删除，默认false。

先以cleanupCandidates/cleanupCheck做只读核验，再决定是否启用purge。定时器或无OPENID服务调用目前不能自动取得管理员权限；不得通过伪造event.OPENID绕过。

管理员需先有经过bootstrap的账号映射，所有接口仍比较实际身份与expectedUserId。回收记录只在受保护资产集合保存，不在公开日志输出私人ID或文件路径。

## 最终测试追加项

1. 两真实账号上传/读取，第三账号、错误成员版本、退出后直接云文件请求均拒绝。
2. 私人云存储规则与服务器上传生成文件的实际创建者元数据相容；确认没有匿名/全员读取通路。
3. JPEG/PNG真实压缩、EXIF/辅助块处理、透明图、尺寸/字节限制、微信原生解码；不能用Pillow结果替代。
4. prepare/upload/finalize任一步丢响应、杀进程、换号、文件丢失及取消后迟到响应。
5. 下载中退出/撤回，15秒租约、页面隐藏、再次进入及错误图片占位；原私人照片仍保留。
6. 并发上传槽位上限、餐食撤回联动、重新共享文字不恢复旧照片。
7. 管理员伪造反例、默认关闭、保留期不足、活跃引用不能删除、失败重试和迟到上传/删除竞争。
8. 真实CloudBase事务及callFunction请求/响应大小限制、函数超时、实际云调用/流量成本。

以上全部统一留到最终执行，不要求用户现在测试，也不构成任何部署/删除授权。
