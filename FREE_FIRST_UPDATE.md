# 免费优先调整

> 后续更新：现已支持本机可自行开关搜索和完整草稿参考字段，见 [IMPORT_SWITCH_DRAFT_UPDATE.md](IMPORT_SWITCH_DRAFT_UPDATE.md)。后端保持用户已确认的开启配置；旧文中的固定客户端开关说明已被替代。

2026-09-11。本说明优先于之前要求为地点搜索升级超时／套餐的部署说明。

## 默认行为

- `miniprogram/utils/importPolicy.js` 中 `cloudPlaceSearchEnabled: false`。Add 隐藏城市输入及云端商家搜索按钮，显示免费优先说明，保留原生地图选点和具体分店确认。
- 服务层在初始化云环境／调用函数之前拒绝商家搜索，因此新版客户端在此路径不产生 placeLookup 调用。无自动重试、定时搜索或付费替代服务。
- 分享文本类别提取、菜系／餐饮类型编辑及待采纳建议继续本地运行。原生选点不承诺提供商家类别与图片；缺失类别手动补充。
- 后端第二道开关：只有 `PLACE_LOOKUP_ENABLED` 严格等于 `true` 才允许请求腾讯；缺省关闭，即使已经配置 Key 和 OPENID 白名单也不会请求腾讯。
- 保留的可选搜索实现将 HTTP 绝对期限从 6000ms 缩短到 1800ms；保留响应大小限制和取消清理。不要求将云函数超时从 3 秒升为 10 秒，不要求升级套餐。1800ms 不是冷启动及 SDK 总耗时保证，实际 3 秒环境仍需验收；默认关闭时不依赖这条路径。

## 你现在需要做

1. 不升级套餐、不为本功能开通额外付费服务。
2. 在原云环境部署新版 `placeLookup`，删除 `PLACE_LOOKUP_ENABLED` 或设置为 `false`。已有 Key 和白名单可继续保存在服务端，不用重建 Key。
3. 若上一轮尚未部署新版 `mealRecords`，仍需部署以保存新类别字段。现有食记保存／照片上传／同步逻辑保留，不清缓存。
4. 在微信开发者工具编译／发布新版客户端。更新后“云端分店搜索”入口隐藏，原生选点可用。
5. 云控制台若有独立的公网 HTTP 访问入口且其他业务不使用，可关闭该入口；小程序 `wx.cloud.callFunction` 不需要为了本功能额外开放公网 HTTP。具体设置请核对控制台，不要误停现有食记服务。
6. 在腾讯位置服务和 CloudBase 控制台核对免费额度、预算告警、自动续费及超额付费设置；若支持拒绝超额／硬配额，优先使用。告警不等于硬性阻止费用。

仅在明确接受额度和计费风险后才考虑同时开启客户端和后端开关；本次交付均默认关闭，不建议为此升级。

## 费用边界与验收

不能仅靠代码保证整个应用永久零费用：现有云数据库、云函数、照片存储／流量仍受服务商额度及计费规则约束。旧版客户端在未更新前仍可能调用已部署的旧函数；即便新版后端拒绝搜索，收到的函数调用本身也可能计入云用量。双开关不是全局计费封顶。

截图显示已配置服务端变量，控制台返回 UNAUTHENTICATED。这能说明无微信用户身份时拒绝请求，不能证明真实账号搜索成功；此次未登录控制台或修改线上配置。

本地及解压包验收：195/195 静态检查、147/147 mock 合约场景通过。新增覆盖免费默认零云调用、原生入口路由、已有凭据但缺少显式开关时零供应商请求，以及 1800ms 超时销毁连接。可选联网路径原有测试继续执行，并未删除。未执行开发者工具、真实云环境或真机验收。
