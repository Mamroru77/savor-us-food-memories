# Savor 总进度：9类业务均已有候选实现

更新：**S4-01，一次完成剩余四类，2026-09-14。**

> **当前是代码候选完成，不是上线完成。** 完整自动回归通过；真实云部署、真实消息投递、微信原生/真机验收尚未执行。

## 总业务进度

| 类别 | 本轮后状态 | 最终仍需核验 |
|---|---|---|
| 1. 可信身份与私人缓存分区 | 候选已实现 | 真账号、离线冷启动、跨账号回调 |
| 2. 双人邀请 / 同意 / 绑定 / 撤回 | 候选已实现 | 真实事务、权限、并发 |
| 3. 主动文字共享 / 独立评分 | 候选已实现 | 实际双账号交互 |
| 4. 共同 Wishlist / 各自意愿 / 转草稿 | 候选已实现 | 真实权限、现有50条索引边界 |
| 5. 私人照片共享 / 媒体生命周期 | 候选已实现 | 私有存储规则、真实压缩/解码、生命周期；purge 默认关闭 |
| 6. 云端批量备份 / 导入 / 任务恢复 | **本批候选已实现** | 500条分块、跨设备恢复、真实平台限额 |
| 7. 云档案 / 偏好 / 最小伙伴资料投影 | **本批候选已实现** | 双设备冲突、真实头像、授权撤回 |
| 8. 年报 / 里程碑 / 分享报告 | **本批候选已实现** | 微信 Canvas、HTML/海报分享与视觉 |
| 9. 一次性提醒 / 反馈真实投递后端 | **本批候选已实现** | 获批模板、订阅、运营渠道、工作器与真实回执 |

**9类都有候选代码；剩余未开发类别：0。** 不换算为工作量百分比；剩余的是统一审查、部署配置、平台/真机验收及其发现的问题修复。

## 本批四类具体完成

### 云端备份与导入
- 最多500条/任务、5条/块；不可变摘要、完整上传后封存、明确确认后恢复。
- 原始 JSON 本机保全；云任务查询、去重、丢回应重试、cursor续传、部分取消、云快照导出。
- **结构化日记快照，不含照片二进制**。导入为私有文本，不覆盖已有云记录、不复活删除标记、不恢复旧照片或共享权限。
- 只读云日记刷新，不顺带发送旧 outbox。

### 云档案 / 偏好
- 明确读云版本、上传本机或采用云端；revision冲突保护与请求回执。
- 白名单字段；设备语言/主题/动效及关系身份不覆盖；采用前保存完整本机分区备份。
- 新选头像缩略图净化；不能用旧 fileID 认领。
- 伙伴仅在明确授权后看到姓名/简介，绑定当前空间、成员版本、档案版本；撤回/换人/重传不自动续授。

### 年报 / 里程碑 / 分享
- 基于当前账号本机真实日记，排除示例、重复、删除/待删除、未来日期；不推断双人评分、消费总额或未知地点。
- 年度统计、月分布、累计里程碑；默认匿名汇总 HTML 与海报，餐厅/城市明细另行选择。
- 不自动上传，不读取伙伴报告；分享后的副本不能撤回。

### 提醒 / 反馈
- 用户手势触发真实微信订阅接口；accept后才安排一次性提醒，不以偏好开关冒充授权。
- 私有云工单 + 生产 HTTPS / 微信 OpenAPI 适配器，受保护工作器与定时器参数入口。
- queued / blocked_config / sending / sent / failed / uncertain / cancelled 持久状态。
- 超时与丢回应不假报成功、不盲目重发；反馈未知结果需渠道明确幂等，管理员核对需留证据。
- **真实消息尚未发送；sent 语义也只是渠道接受，不是用户已读。**

## 页面入口

- **Me → 云端工具 / Cloud tools**：备份、导入、偏好、提醒与反馈及任务恢复。
- **Me → 年度报告 / Annual reports**。
- Help & Feedback 保留本机笔记，新增云端反馈入口。
- 新增均为非 Tab 页面；五 Tab、正式 UI、地图与原有草稿/照片业务保持。

## 自动化证据

最新完整日志：`CURRENT_TEST_RESULTS.txt`，`npm run verify:all` **退出码0**。

| 检查 | 结果 |
|---|---:|
| 原有静态主检查 | 297/297 |
| 原有业务 mock | 233/233 |
| 身份运行时 | 22/22 |
| 双人空间 / 文字 / Wishlist | 26/26 |
| 共享媒体后端 / 客户端 | 30/30 |
| S4 备份 / 档案 / 资料投影 / 投递 / 客户端 / 报告 | 48/48 |
| S4 生产适配器线格式 / ack / 定时器路由 | 5/5 |

其余 Tab、资源、图标、handoff、UI 与自动采集工具检查仍在完整回归链内。
**新增53项通过，全部为本环境自动化；不是真实云/原生/消息送达证据。**

## 下一阶段：一次性统一验收

唯一总清单：**`UNIFIED_ACCEPTANCE.md`**。本批实现与范围：`S4_IMPLEMENTATION.md`。

1. 独立代码审查与本地微信编译/模拟器自动化。
2. 获授权后协同部署云函数、集合、私有权限、模板/渠道/工作器。
3. 统一两账号和真机验收，集中修复失败项并再次回归。
4. 回退演练，另获授权后才提交/推送/发布。

本轮没有云部署、权限修改、真实数据/图片删除、实际消息发送、Git提交/推送或用户本机工程覆盖。
Tab微抽动与商家万能导入继续暂缓；不恢复K1及已失败的实验。
