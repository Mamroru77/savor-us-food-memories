# Tab 导航与动画分离修正版

日期：2026-09-13；基线：b67bf8c（源码等同94585cb）。

**本轮状态：代码与模型通过，真机待复测。重点修正导航可能被动画拦截、图标命令可能错配的路径，不宣称原始回跳已全部解决。**

be6eaf2曾导致真机Tab点击无响应，已撤回。本版不是重新应用其owner方案。

## 1. 导航不再依赖动画与视图准备

有效点击先记录tap，再调用wx.switchTab。删除以下导航前置条件：

- 推断出来的owner；本版不建立或锁定owner。
- 上一次动画/访问的pending状态。
- this.data.selected等可能过期的显示状态。
- 父/子setData确认、隐藏图像load和800ms准备计时器。

仅无效索引会被拒绝；与当前实际路由相同的点击记为tap-noop。两种情况都有明确reason。路由暂不可读时，不用显示选中项阻止有效点击。

目标路径取自既有五Tab列表。导航失败或同步异常会记录，不留下禁止下一次点击的锁。旧回调不能重置较新请求的记录。

切页前不再把目标状态广播给所有实例，也不提前改变源页图标。本轮仅移除代码等待，不保证微信原生切页耗时为零。

## 2. 移除双重命令分发

原来同时存在：

1. WXML `presentation="{{viewState.icons[index]}}"`。
2. 父组件查询子组件后，按返回数组位置再次调用acceptPresentation。

如果查询结果存在缺项，第二条路径可能把Home命令发给Add；相同revision检查又可能阻止后续正确命令。本轮直接移除第二条路径，不再假设组件查询数组位置等于Tab身份。单对象命令及已有旧帧保护仍保留。

该条件反例可在JavaScript中复现；尚未证明真机录屏实际发生了查询缺项。因此这是一项明确的防错修正，不是完整的原生根因证明。

## 3. 新实例与首次文案

目标实例按实际路由取得本次访问的from/to/key，首次发布为active:true，不要求此前已有同key激活记录，也不采用固定owner。

五个页面在访问getTabBar前保存只含语言标签、主题和Quiet的UI元数据。晚创建实例可读当前页面的UI元数据；i18n刷新维护同样的数据。首次可见提交带上标签，不先显示默认英文再补中文。

仍不能用实例创建编号推断谁实际可见，也不将日志最后一个bar默认解释为当前页面返回的对象。

## 4. 诊断现在区分导航、实例和图像

继续保留最近96条纯UI内存事件，不上传、不保存业务数据。

- tap：点击进入处理函数、目标索引、实际路由及显示selected。
- tap-noop / tap-rejected：明确原因。
- switch / switch-success / switch-failed：API调用与返回。
- view-commit：当前发布的视图回调。
- static-loaded / static-load-ignored：静态图像load及URI不匹配。
- 图标报告带访问key，static-fallback保留具体reason。

新增诊断入口返回**本次查询对象自己的bar编号**，以及全局trace。不能再把全局trace最后一条的bar编号当成当前可见实例。

```js
(function () {
  var page = getCurrentPages().slice(-1)[0];
  var bar = page.getTabBar();
  return JSON.stringify(bar.getTransitionDebug());
})()
```

该版输出schema为nav-independent-v1，并包含route、routeIndex、bar、selected和trace。

## 5. 保留与未闭环

保持五组官方图标、尺寸和配色，480ms形变预算、SVG逐步推进、首帧保护、Quiet及错误降级不变。Map 320ms、照片地标、堆叠按钮和业务/API均保留。Map文件只更新Tab的外观元数据发布代码。

本轮没有关闭静态图像1200ms超时保护，也没有擅自改成无限重试。其真机触发原因还需要新的reason/load记录确认。不同实例降级状态不一致导致的显示回跳仍是待验证风险。

原生页面空白、跨实例实际显示顺序、真实形变耗时均未在工作区获得真机验收。配置480ms和测试帧预算不能证明实际观感相同。

## 6. 验证

完整源码：229/229静态、21/21 Tab接入、233/233既有mock、35/35交接回归、42/42 UI；48官方SVG、28别名、73图标声明。交付前另验证实际ZIP解压副本。

对照反例：

| 条件 | 回退版 | 已撤回owner版 | 本版 |
|---|---:|---:|---:|
| 错误owner时点击其他实际路由，调用次数 | 1 | 0 | 1 |
| 视图更新抛错时点击，调用次数 | 0 | 1 | 1 |
| 前次未回调连续两次有效点击，调用次数 | 1 | 1 | 2 |

这些是代码层API调用测试，不代表微信会接受所有并发请求，也不代表实际页面已显示。原有动画几何、反向、Quiet、错误、业务及Map测试没有删除；过时的“导航必须等待隐藏实例”断言按本轮要求替换。

## 7. 真机验收顺序

1. 先验证Home、Map、Add、Us、Me之间能正常切页，不把动画状态作为导航成功标准。
2. 同一会话不退出，重复Add→Us→Me→Add，覆盖缓存回访及稍快连续点击。
3. 再看回跳、空白、英文标签和动画是否完整。
4. 如有异常，提供对应录屏及上方新诊断输出。先判断tap是否到达、switch是否调用/成功，再分析图像问题，不继续把owner推断或等待锁接到导航前面。
