# Savor 统一修复与验证结果

日期：2026-09-15  
目标：AppID `wx65867b5568b82996` / 环境 `cloud1-d9gqm52id66c0bcda`

## 本次结论

上一轮隔离任务已经成功完成，因此收到“重试”后先读取了结果，没有重复写入。**本轮保存提示修复、反馈仅存储策略、部署、回归及隔离云端验证已完成。** 这不等于全产品的真机、双账号或生产验收已经完成。

你的两项决定已落实：
- **提醒保持关闭**，不是等待你补模板。
- **反馈仅存自有私有云端工单，不向第三方外发**，不是等待你配置第三方接收端。

## 1. 保存问题：错误提示已修复，默认冻结不变

当前诊断包仍不允许实际保存或同步，这是此前选择的默认策略；本次没有擅自解冻。

已明确区分“身份验证失败”和“诊断模式保存冻结”。保存按钮显示“保存已冻结”；会话失效和草稿持久化失败有明确错误处理，不再一律要求反复联网验证，也不虚报保存成功。

前批实际 Add 保存处理器验证：冻结原因与短标签正确、当前草稿不变、云调用 0 次。原有五 Tab、表单布局、地图与身份约束保留。手机旧预览包是否已更新尚未核验，不能将工程修复说成手机已验收。

## 2. 反馈仅存储：不靠“暂时没配渠道”来保护

- 生产服务明确配置 `feedbackMode: cloud_only`、`remindersDisabled: true`。
- 新反馈工单状态为 `stored`，带有 `deliveryMode: cloud_only`；不标记为 `sent`。
- 工作器和重试接口都不能将仅存储工单转为外发；以后修改渠道配置也不会释放这些工单。
- 未明确标记外部投递授权的旧任务不会被工作器自动发送。未删除或自动重放原队列。
- 客户端确认文案说明正文与可选联系方式仅随私有工单保存；请求也明确携带仅存储模式。
- `feedbackStorageReady` 与 `feedbackReady` 分开表示存储能力和外部投递能力。

真实生产服务读取验证通过：仅存储模式生效、提醒关闭、提醒创建请求被拒绝、未授权工作器被拒绝、任务列表可读。验证时正式投递任务为 0，旧本地日记与草稿不变。

## 3. 自动回归与部署证据

| 验证项 | 结果与边界 |
|---|---|
| 最终 `npm run verify:all` | 退出码 0，已包含新增反馈策略测试 |
| 新增仅存储策略回归 | 14/14；隔离模拟事务，无网络 |
| 原 workspace / report 回归 | 48/48；模拟传输，不是真实投递 |
| 原生产投递适配器回归 | 5/5；无网络，保留未来功能的代码覆盖 |
| 保存冻结定向回归 | 前批 8/8；模拟/静态检查 |
| 实际锁定 SDK 事务适配检查 | 前批 18/18；拦截传输，无真实网络 |
| 最新 Workspace WXML | 官方编译接口通过 |
| 生产 workspace 部署 | 回下载 index、handler、package、锁文件与工作区逐字节相同 |

微信开发者工具曾出现模拟器无响应和 `getRootFactory Not Define Env`。通过正常退出、重新启动目标工具恢复；未清理缓存或用户数据。恢复后重新取得编译通过和真实云端策略结果，没有把“刷新命令已受理”当作编译成功。

前批已验证的 account → handshake → 私人分区链路、11 个业务集合客户端读/更新拒绝、11 项基础云端用例继续作为独立历史证据，不冒充本批新增测试。

## 4. 真实隔离云端验证：206 次调用成功

使用单独集合 **`savor_audit_20260915`**，创建时直接设为所有客户端不可读写；客户端读与更新均实测得到 `-502003`。

测试调用真实云函数及 CloudBase SDK / 数据库事务。复用与生产一致的 workspace handler、schema、image 代码，使用专门的仓储映射将所有逻辑表限定到这个测试集合。**这不是直接在生产业务表运行，也不是两真实账号验收。**

- 206 次调用全部成功，总耗时约 **179 秒**。
- 500 条模拟记录：100 个分块上传、封存、确认恢复、100 步恢复完成。
- 最终状态 `done`；游标为 **100 个分块**，创建、计数与逐条检查均为 **500 条记录**。
- 500 条均为明确标记的模拟私有记录，无照片。
- 实际事务写入后注入错误，验证回滚没有留下档案或回执。
- 档案写入与回读通过。
- 反馈工单保存、相同操作号重试不重复创建、拒绝转为投递、未来渠道配置下仍不外发，均通过；发送次数 **0**。
- 原 `savor-diary-v1` 与 `savor-draft-v1` 序列化值不变；默认诊断冻结仍为 true。

隔离驱动预检曾发现测试驱动把分块游标误当作记录游标；在任何真实测试写入前修正。没有因此修改生产恢复算法，也没有将这项预检错误说成生产故障。

## 5. 收尾与数据保全

- 测试集合最终为 **605 个测试文档，899,665 字节**，包括 500 条模拟记录和测试分块、工单、档案等辅助文档。
- 原有 **25 个集合的记录数和数据大小均与测试前一致**。这项元信息核对不是逐文档内容比对；测试驱动的单集合约束另有代码和预检证据。
- 临时测试云函数已部署为关闭状态，下载代码比对通过。
- 使用原测试凭据再次调用也被 `AUDIT_CLOSED` 拒绝。
- 本批生成的临时测试凭据文件已清除凭据内容；未改动产品身份密钥或现有运营凭据。
- 保留私有测试数据作为证据，**没有执行云端删除**。测试入口关闭，未配置定时器，未升级套餐，未推送 GitHub。

## 6. 仍须集中真人验收的内容

1. 两真实账号邀请、接受、退出、并发、旧授权失效及第三账号拒绝。
2. 私有媒体的实际文件创建者/ACL、退出后访问拒绝，以及相机、相册、定位等原生授权。
3. 键盘、安全区、图片解码、分享、海报、文件选择及杀进程恢复等真机生命周期。
4. 经明确选择切换正常模式后的手机保存与同步。当前默认冻结包不能用于宣称该项通过。

提醒和第三方反馈投递不在本轮启用范围，不再要求你提供相关模板或接收端。隔离 500 条测试也不能替代真实设备容量、双账号和长期并发负载验收。

## 工程与证据位置

Windows 最新工作区：
`E:\HuaweiMoveData\Users\HUAWEI\Desktop\savor-audit-repairs-20260915`

- `reports/mcp-unified-evidence/cloud-only/`：策略回归、生产部署、编译与真实配置验证。
- `reports/mcp-unified-evidence/isolated/`：隔离 ACL、206 次调用结果、数据保全、关闭入口证据。
- `tools/verify-feedback-policy.cjs`：已接入常规回归的新增策略检查。
- `docs/reviews/mcp-cloud-only-isolated-20260915.md`：本次最终报告。

未用旧 ZIP 覆盖最新工程。历史报告中“缺少提醒模板/反馈接收端”和“500 条云端测试未执行”的状态，已由本报告中的最新选择与证据更新。
