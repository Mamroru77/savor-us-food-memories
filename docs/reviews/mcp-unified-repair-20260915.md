> **最新状态：提醒按用户选择关闭；反馈仅存私有云端工单策略已部署。新增14项策略回归、最终全量回归、真实生产配置检查及206次隔离云调用通过，500条模拟恢复完成。测试入口已关闭，原25集合记录数/大小未变。默认仍冻结，真机与双账号验收未完成。详见 docs/mcp-cloud-only-isolated-20260915.md；历史待配置/未执行条目以最新报告为准。**

# Savor 统一修复与云端验证状态

日期：2026-09-15。AppID `wx65867b5568b82996`；环境 `cloud1-d9gqm52id66c0bcda`。

## 结论与边界

本批已完成保存阻塞提示修复、事务适配器修复、三项新增云函数部署、十个私有集合补建，以及真实身份/权限/基础事务读取验证。**不是“全业务已验收”，也不能宣称只剩真人测试。** 提醒及反馈的真实投递配置尚未完成；生产写入、共享媒体及大规模恢复仍须安全的隔离验证或真实账号验收。

默认 `identityMode: diagnostic` 继续保留。没有恢复自动同步、发送 outbox、投递消息、上传私人照片、删除云数据或推送 GitHub。

## 一、截图中的保存问题

最新工程中，身份验证成功与允许业务写入是两个不同状态。诊断验证可以成功，但业务保存仍按既定策略冻结；旧代码将这种情况也提示为“请联网验证”，容易误导。

已修复：
- 冻结状态明确显示“诊断模式：保存与同步已冻结”，不再将它说成网络错误。
- 保存按钮使用简短的“保存已冻结”，并声明禁用状态；即使事件被调用，处理器也会先阻止写入并给出行内说明，不重复发泛化 toast。
- 身份诊断按钮区分首次与已尝试状态，避免反复点击无效重试。
- 普通模式会话失效时，保存入口捕获身份错误，不再直接抛出。
- 草稿持久化失败会明确提示“尚未确认保存”，保留当前可见输入，不虚报已保存。尚未持久化的输入不保证离开页面后仍在。
- 保持原有五 Tab、Add 表单布局、地图、图标和数据边界，不绕过身份校验。

**微信开发者工具真实运行时验证**：进入 Add 后调用实际保存处理器，冻结原因正确、简短标签正确、草稿前后相同、云调用 0 次。此项验证的是拒绝路径，不是正常模式成功保存。用户手机具体包版本尚未核对，不将 IDE 结果写成手机验收结果。

## 二、真实身份链路

- `account → mealRecords.identityHandshake`：成功，返回同一身份；错误身份请求被拒绝。
- 本批进一步运行实际 `identity.verify({diagnostic:true})`：阶段 `complete / OK`，成功接入私人分区，仍保持冻结。
- 完成时间 `2026-09-15T13:01:30.869Z`；该次 account + handshake + 分区接入总耗时 5931 ms。
- `savor-diary-v1` 与 `savor-draft-v1` 原键序列化值前后相同。没有清空原数据、重置产品身份或迁移后自动上传。

## 三、代码与部署

### 修复了另一处实际 SDK 契约问题

`spaces / media / workspace` 原事务适配器把 `-502004` 当作文档不存在，并使用过宽的错误文本匹配。实际锁定 SDK 中，`-502004` 对应集合数量超限；集合缺失错误也可能被宽泛匹配误判为空。

现在使用 SDK 支持的 `cloud.database({throwOnNotFound:false})`：仅成功的空文档结果返回 null，集合缺失、权限、额度、参数错误继续上抛。没有退回非原子写入，没有升级 SDK。

### 云端状态

| 项目 | 已完成 |
|---|---|
| spaces、media、workspace | 已部署；回下载的 index、handler、package 和锁文件与本地逐字节相同 |
| SDK | 三函数均为 wx-server-sdk 2.4.0；锁文件复用先前检查的依赖图 |
| mealRecords | 回下载 index/schema 与当前工作区相同；本批未重部署 |
| account | 沿用上一批已验证修复，本批未重部署 |
| workspace 超时 | 已从新建默认 3 秒设为 60 秒，元信息回读 Active/60；对应其约 40 秒工作器预算 |
| account/mealRecords/其他函数配置 | 未借机修改；未更改内存、运行时、套餐 |

初次新建三函数后，上传代码被 Creating 状态拒绝；检查 Active 后重试才获部署成功。没有把第一次 CLI 的退出码 0 当作部署通过。

## 四、集合与权限

已补建：
`savor_invites`、`savor_spaces`、`savor_wishes`、`savor_shared_meals`、`savor_media_assets`、`savor_archive_tasks`、`savor_archive_chunks`、`savor_profiles`、`savor_workspace_receipts`、`savor_delivery_jobs`。

- 创建时直接选择“所有用户不可读写”，没有先放宽权限运行。
- 最终元信息确认十个新集合均存在、记录数均为 0；原账户集合仍为 1 条。
- 加上 `savor_accounts`，11 个集合的客户端读取与非 upsert 更新，最终均实测拒绝，错误码 `-502003`。
- 未改动 `dining_records` 或原有存储规则，未创建 ACL 测试文档。
- 第一轮实测发现 `savor_spaces` 尚未真正落地（`-502005`），安全检查当即阻止后续业务调用；补建并用独立元信息/客户端测试确认后才继续。因此不把初次点击或截图当作创建成功。

UI 自动化经历了加载中、焦点不符、渲染帧未更新等情况；提交前检查失败时均停止。导航期间控制台默认显示过非目标记录，未修改；临时整窗图后续已被无记录/权限页面覆盖，不纳入交付。

## 五、回归与真实云端验证

| 证据层次 | 结果 |
|---|---|
| 完整 `npm run verify:all` | 最终退出码 0 |
| 新增保存/冻结定向检查 | 8/8，模拟与静态检查 |
| 实际 SDK 事务封装检查 | 18/18，底层事务传输拦截，真实网络 0 |
| 官方 WXML 编译 | Add、身份门禁、云端工具、年度报告 4 个模板通过；Add 最终版另行重编译通过 |
| 真实身份接入 | account、handshake、分区接入成功；旧键未变 |
| 真实客户端数据库 ACL | 11 集合 × 读/更新：全部拒绝 |
| 真实云端基础用例 | 11/11 符合预期：5 个正常读取、6 个预期拒绝 |

11 个云端用例：spaces.status、无效邀请查询；workspace.config/getProfile/listTasks/listJobs；不存在媒体查询；未授权 worker；spaces/media/workspace 各一次错误身份请求。

这些调用真实执行了云端身份鉴权和事务读取，但没有覆盖两人绑定成功、共享媒体上传、正常日记写入、500 条真实恢复或真实消息投递。不得把拒绝用例计为正向业务成功，也不得把拦截 SDK 测试计为真实数据库事务验收。

## 六、剩余项：明确区分，不掩盖

### 外部配置 / 安全决定（不是真人测试）
1. 提醒：实际获批订阅模板及其字段、云调用权限、获批的工作器身份与调度配置。
2. 反馈：由用户指定可信接收端及运营方，确认允许传输反馈正文和可选联系方式；密钥只能通过受控配置渠道处理。不能任意选择第三方接收者。
3. 当前真实 workspace.config 返回 `reminderReady=false`、`feedbackReady=false`。未配置状态不会伪装为投递成功；没有启用定时器。
4. 正向批量写入及恢复压测需要隔离数据方案。没有为了测试往用户正式日记塞入 500 条假回忆；大规模云端回归和查询计划验证不能以空表读取或离线测试代替。

### 集中真实账号 / 真机验收
- 两账号邀请、接受、退出、并发与旧授权失效；第三账号拒绝。
- 私有文件实际创建者/ACL、退出后直接文件访问拒绝、照片压缩与原生解码。
- 相机/相册、定位、键盘、安全区、文件选择、海报及分享的原生生命周期。
- 换号、断网、杀进程、跨设备、实际消息授权与收件。
- 正常使用版本的保存与同步验收。当前默认冻结版本不能用于宣称这项已通过。

## 交付位置与回退

Windows 工作区仍是 `E:\HuaweiMoveData\Users\HUAWEI\Desktop\savor-audit-repairs-20260915`。证据与逐文件备份在 `reports/mcp-unified-evidence/`，新回归脚本在 `tools/verify-save-freeze.cjs` 与 `tools/verify-transaction-adapters.cjs`。

此前 ZIP 未覆盖重导。本次补充文件不是完整发布包，不能拿它覆盖未知新改动。回退代码也必须保留身份/权限约束及全部数据，不得删除新集合、账户映射或用户私人文件来“恢复环境”。

总进度：8/8 原本地审计修复、9/9 功能候选的历史口径不变；新增云端部署、私有配置、基础真实验收已完成上述范围，整体生产验收仍未完成。
