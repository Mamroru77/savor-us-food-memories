# SAVOR · R2 执行与验收报告

**2026-09-18｜工程改动已落盘，全量代码回归通过；完整 R2 验收尚未完成。**

这不是“全部完成”声明。仍有原生连续路径、四档真实视口、真机及任务定义缺失的阻塞，另有受保护草稿与交接描述不一致的现场情况。没有把这些项目改成 FIXED，也没有用合成测试冒充真人或真机验收。

## 1. 全量基线与最终回归

- 接手全量基线曾退出 **1**：Me 页已保留的 A 原图返回逻辑，与旧菜单检查的“菜单外源码逐字不变”约束冲突。
- 保留历史 fixture 和原断言；新增限定于已审阅 A 改动的源码投影。投影先校验 Me 的精确 SHA-256，再逐块匹配；陌生源码、路由、菜单或恢复逻辑改动均拒绝。负向突变测试及原 Memory return 套件通过。
- 修订基线：`r2-full-baseline.exit = 0`。
- **最终 `npm run verify:all`：退出码 0**，日志完整执行到 `verify:r2` 七个专项入口。
- 最终证据：远端 `reports/ux-remediation-20260917/r2-final-verify-all-4.log`、同名 `.exit`。
- `git diff --check` 退出 0；小程序源码诊断 **0 error / 0 warning**。
- 先后遇到的原审阅路径约束、分页节点重复约束，均通过调整审阅记录或产品模板解决，**没有修改这些断言**。

## 2. A｜Memory 原图返回：KEEP（继承）／PHONE VERIFY

A 的原有实现保留，原 Memory return、身份恢复与页面生命周期回归均通过。

本轮真实元素操作能从 Map 打开 Memory，截图确认从照片起点显示；但组件内原图点击无法由当前 CLI 选择器定位。尝试的 Windows 原生输入被前台窗口安全检查阻止，未绕过检查。因此**没有取得本轮“原图 → 原生返回 → Memory → Map”的完整连续验收证据**。

## 3. B｜Add 取消与草稿：BLOCKED（现场不一致）

- Add 原生取消专项仍通过，未修改本轮接手的 Add 业务源码。
- **首次只读核对时，当前已验证运行实例的 `store.loadDraft().restaurant` 为空，并非交接文档要求保留的 `111`。** 这发生在本轮受控偏好矩阵之前；矩阵期间及恢复后，草稿内容比较均未改变。
- 本轮没有调用草稿写入、清空或保存餐食，也未选择/保存真实地点候选。
- 不能据此断言旧草稿已被删除：身份分区封装下，原始 wx storage 键是否存在也不是充分证据。当前实例是否与交接时的草稿上下文一致，尚未查明。
- **没有伪造一个新的 `111`、回填未知归属数据或绕过身份分区。此项不能签署保留验收。**

证据：`r2-final-draft-integrity.json`、`r2-matrix-and-draft-baseline.json`、`r2-closeout-readonly.json`。

## 4. C｜Map 空态语义：KEEP（继承）

保留已有原因区分及恢复入口；原 **11 项** Map 空态专项通过。没有为制造空态而清空真实记录，也没有把这轮合成空态测试写成新的真机证据。

## 5. D1／D2／D3｜地图点击、展开、选中

### D1、D3：KEEP；真机仍需验证

实际元素操作重新执行：展开 → 选中子项 → 全览。只读比对表明：卡片与选中 ID 一致，点全览前后保留同一选择。没有顺带改选餐厅或请求用户 GPS。

### D2：PARTIAL；方向修复与限定路径已验证

- 新增 `verify-map-search-clearance.cjs`，直接执行生产 `buildDrawers()`；先取得 **13 通过、18 失败**。
- 旧路径：展开按钮 top **155.05px**，侵入 **208px** 安全线之上；截图确认被 Search 遮挡。
- 新实现按完整容量选择上下方向；同步调整原生 callout、行、分页和真实命中区，不翻转照片、不修改真实坐标。
- 保留根锚点、固定边界、320ms 动画及既有向上模型；没有拖拽期间重建相机、marker 或 canvas。
- 新专项 **31/31 通过**，含 320／375／428 宽度、不同子项数、半展开、短末页、根锚点及原生/普通命中几何一致性。原 motion、viewport 断言未改且通过。
- 新原生截图及几何读回验证了一个实际向下展开路径；最新全览路径展开后的按钮 top 为 **304.42px**，安全线 **208px**。
- **未穷尽屏幕边缘、底部卡片空间不足、所有页数及所有设备视口，因此不标为完整 FIXED。**

证据：`resume-d2-before.jpg`、`r2-d2-after.jpg`、`r2-final-map-overview-direction.jpg`、`r2-final-map-hit-audit.json`。

## 6. D4｜图钉照片恢复：PARTIAL

复现并修正一个确定根因：一次临时图片加载失败，被缓存成“已经就绪的 fallback”，后续请求不再尝试照片。

现仅逐出失败任务的缓存；后续已有渲染请求可重试。成功缓存、单飞队列、隐藏/销毁保护继续保留，未增加轮询或第二个 renderer。

新专项验证：失败后恢复、成功缓存不重绘、并发单飞、销毁后禁止回写、迟到临时文件清理、新 renderer 重建。真实 Map 路径中也读到展开根图和子图已合成，但**没有完成原生预览返回的全部照片恢复路径，更没有承诺不存在权限或原始文件不可用的 fallback**。

## 7. D5｜连续地图路径：PARTIAL

已取得实际元素操作与截图：全览、展开、子项选择、保留选中的全览、打开 Memory。

未闭环部分：

- 原生照片预览及返回：组件选择器不可达，桌面输入安全检查阻止。
- 拖动：CLI 的 touchstart/move/end 返回 success，但 **getRegion 前后完全一致**，故不能认定地图发生了真实拖动。
- 缩放、真实多页栈翻页及完整连续往返：未验收。

没有用 Page 方法调用或移动相机 API 冒充手势和真人路径。

## 8. E｜实际能力与共享表达：PARTIAL

完成源码映射、两种语言文案和专项回归：

| 状态/操作 | 实际能力 | 修订表达 |
|---|---|---|
| Memory 的 shared 标记 | 当前账号的日记分类，不代表另一账号获授权 | 加入「我们」／已加入「我们」；Show in Us／In Us |
| 移除标记 | 从本合集移除，不撤回另行授予的空间权限 | Toast 明确“空间访问权限未改变” |
| 双人空间 | 独立的同意、授权及显式分享机制 | 保留管理入口及服务；解释与合集的区别 |
| Together、用户填写的关系称呼 | 不能冒充已验证的账号绑定 | 保持用户输入，不构造连接状态 |

修正了旧 FAQ“绑定/发送未启用”的过时说法。Together、Journey、Shared Moments 结构保留，没有真实 Share/Love/Like/邀请/接受/绑定操作。

实际矩阵发现新增说明曾与按钮同行，已改为独立块并重截图检查。更全面的长文案/尺寸及原生共享状态验收仍待完成。

## 9. F｜细项

| 项目 | 当前结果 |
|---|---|
| F1 | **PARTIAL**：Reports 原生标题、海报和可选 HTML 均支持 locale；主页面及 Reports 的 0/1/2 单复数和英文标点已修订。保留中文默认调用兼容；用户餐厅、备注、城市不自动翻译，HTML 转义和明细同意边界保留。专项、生命周期以及最新英文/Dusk 截图通过。未声称穷尽全产品文案。 |
| F2–F4、F6 | **BLOCKED**：R2 仅写“未执行”，没有各项完整验收定义；原始 `SAVOR_HANDOFF-20260917.md` 未找到，不能自行编造缺失任务。 |
| F5 | **PARTIAL**：从“6/8 批次提示”推进为每页四条＋上一页/下一页，始终不超过原生六项上限；0/1/6/8/17 条、全部可达、返回和过期身份检查通过。没有选择或保存真实地点；原生弹层及真机仍未验收。 |
| F7 | **PARTIAL／PHONE VERIFY**：全览按钮改用已有官方 map-pinned 图标，标签明确“全览已定位的回忆”。保留选择及 All-overview 行为，无新增 GPS、相机或 Tab 逻辑。专项及实际全览点击通过。 |

F1 原回归没有通过删除旧 fixture 解决：复用现有 presentation helper 的 i18n，避免向 Reports 页面加入历史 harness 不接受的新依赖。每次显示仍只有原来两次报告 build；隐藏页不会改其他页的原生标题。

## 10. 矩阵、最终状态与边界

### 已执行的受控真实渲染矩阵

- **48 次**：中文/英文 × Pearl/Dusk × Quiet 开/关 × Home、Map、Add、Us、Me、Reports。
- 实际查询到的统一视口为 **390×844**，不是四台设备。
- 发现并修正 Us 说明布局、`1 days`、英文 Reports 海报仍为中文等问题。
- 修订后对 Us 与 Reports 再执行 **16 次**，抽查新英文/Dusk、中文/Pearl截图确认相应问题消失。
- 通过集中式 `store.updateSettings` 仅调整语言、主题、减弱动效；每次检查真实 memories/outbox/draft 未改变。属于**受控渲染测试，不冒充 Settings 菜单的真人点击验收**。
- 已恢复 **system／Pearl／Quiet 关闭**，并返回 Home。当前读取到 12 条记录；不是合成、注入或补造的记录。

### 未完成的验收

- 四档受控原生视口：当前 CLI `automation_viewport_action --help` **没有 resize/device 操作**。R2 对该能力的乐观判断未由现场证实；没有将桌面窗口大小或 mock 系统信息冒充设备矩阵。
- 原生连续路径与真机验收，以及 F2–F4/F6 的定义恢复。
- 受保护草稿 111 的运行实例/身份分区一致性确认。

### 保留与禁区

- HEAD 保持 `2b162558deca71e68021d89e2a4005b359bb540c`；没有 commit、push、merge、发布、云函数部署或生产权限改动。
- 未恢复被否决的 Tab/handoff/switchTab/Morphicons 优化。
- 没有真实邀请、绑定、点赞、反馈、导入导出、候选保存、照片上传、清库或迁移。
- `VID_1.mp4` 仍存在、未跟踪，未修改；未删除真实 Memory 或原始照片。
- 历史 fixture 及 applied-hashes 历史未改写。审阅链和精确源码哈希位于：
  - `docs/mcp-baseline-guard-review-20260918.md`
  - `docs/mcp-r2-change-review-20260918.md`
  - `docs/reviews/ux-remediation-20260917.md`
- 七个 R2 专项已注册到 `verify:all`；CI 不依赖 ignored 报告目录。

**最终判定：工程修复与代码回归取得实质结果，但“R2 全部完成”的验收条件未满足。以上阻塞保留为未完成，不作完成通知或发布批准。**
