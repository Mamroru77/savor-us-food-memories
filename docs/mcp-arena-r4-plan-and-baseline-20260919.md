# R4 review reconciliation — 2026-09-19

Baseline verify:all exit 0, HEAD 2b162558deca71e68021d89e2a4005b359bb540c, integrated-with-cloud. Start Home390/system/Pearl/Quietfalse,12 memories/outbox0,loaded draft restaurant empty.

Requirements are now the supplied 真人路径UIUX审查报告.md, canonical P1/P2 IDs; F numbering is not invented. Fresh source confirms P2-02 Memory has no restaurant identity node, P2-05 retry remains primary for an empty sync queue, and P2-04 Cloud tools leads with controls/technical output. Preferences is a candidate: inspect before any change. Map night veil is rgba(0,0,0,.3), not merely a class name; it conflicts with the new explicit base-map treatment restriction and needs a narrow removal contract, without camera/marker changes.

Step3 isolation: all attempted paced native inputs were blocked by the foreground ownership guard (actual133324 versus project14225702). Unchanged getRegion is NOT evidence of a product gesture regression or success. No guard bypass and no product timeout/gesture workaround. Continue independent work; native closure remains contingent on safe foreground access.

Draft: located and read reports/user-path-audit-20260916/081-current-draft-readonly-result.json and its original read-only function. Historical active partition owner/namespace matched, chunk integrity passed, persisted 111/2026-09-16/rating0/photos0 while Add displayed empty. Re-executing the SAME read-only function now confirms valid owner/namespace and integrity but persistedDraft.present=false. Historical output contains no raw account ID, so cross-time identical owner cannot be proved just from both ownerMatches booleans. No recovery writes; original 085 screenshot is preserved separately.

Next: separate red/green contracts for Memory identity, Sync hierarchy, Cloud tools presentation and map veil removal. Keep all existing bindings/services and history assertions. Runtime captures are not a substitute for failed native input. No business mutations.
