# 地图展开按钮点击链路修复

2026-09-11。用户开发者工具实测：直接调用 onDrawerToggle 后 open 从 false 变 true、progress 从 0 变 1、height 从 129 变 230、rows 从 0 变 1，画面显示两项。该证据确认新版代码运行、分组存在、程序展开及终态渲染可用；不能据此证明原生手势或实际中间帧已经验收。

本次针对“程序能展开，手点无响应”：
- 删除覆盖箭头按钮的内层 cover-view，让可见箭头文本与 catchtap、data-index 同在一个组件，不依赖原生 cover 子元素的事件传递。
- 子图标和首个图标的 cover-image 叶节点显式绑定 catchtap 和 data-id，外层也保留点击区域。
- 增加仅展开时显示的普通 view“收起标记”入口，位于搜索栏下方左侧；即使列顶箭头被页头遮挡，也能收起。此项是可操作性兜底，不声称已实现地图自动避让。
- 不更换图标素材，不改变320ms双向动画、坐标、私有照片权限、食记存储或Me。图标素材与用户参考图差异仍待单独解决；本次不能宣称两个问题全部完成。

验证：196/196 静态、175/175 mock 通过，ZIP 解压后复测通过。新增覆盖可见节点的WXML绑定结构，以及箭头／子图／首图／独立收起入口的调用。Node 测试不模拟原生 cover 事件派发：本次叶节点修复仍需用户手点验证，不能直接称已确认唯一根因。

仅客户端更新，无需部署云函数、不清缓存。建议完整编译后直接点击箭头（不要先运行Console代替点击）：确认展开；点击子照片切换底卡；用列顶箭头及“收起标记”分别收起。若仍不触发，应采集真实事件，再决定是否更换交互承载层。
