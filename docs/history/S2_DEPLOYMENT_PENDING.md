> **2026-09-15 统一修复最新状态：保存冻结提示已修复，云端基础链路已实测。**
> spaces/media/workspace 已部署并下载比对，10个新集合已安全补建；11集合客户端读/更新均拒绝。真实身份接入与11项云端基础用例通过，全量离线回归通过。默认仍 diagnostic；没有恢复同步/投递/删除/推送。提醒与反馈外部配置、隔离写入/批量恢复及集中真机验收尚未完成。最新报告：docs/mcp-unified-repair-20260915.md。以下为保留的历史阶段记录。

# S2 待最终统一执行的部署事项

**只记录，不执行。** 仍需用户授权，不能创建真实集合、修改权限或部署。

1. 保留S1的 account/savor_accounts/受保护mealRecords。
2. 待授权新增并部署 `spaces` 云函数，依赖 wx-server-sdk ~2.4.0。
3. 待授权创建 savor_invites、savor_spaces、savor_wishes、savor_shared_meals；客户端直接读/写均禁止，服务端函数按真实身份和事务检查访问。savor_accounts 同样禁止直读写。
4. `db.runTransaction` 是必需能力。实际部署前核对SDK的事务/doc.get/doc.set行为、事务操作量/冲突重试、文档不存在错误码。**不能用多个普通写操作替代事务**。
5. accounts新增 activeSpaceId、pendingInviteId、membershipVersion、lastInviteAt；不要清空或重新生成产品用户ID。
6. 不修改dining_records的owner-only私人权限，不公开云存储，不给客户端直接查邀请token或成员映射的权限。
7. 共享读取在页面打开期间约15秒复核一次，产生真实云调用和事务读取成本；免费优先不代表零用量。每空间索引限额见S2实现说明。
8. 文本发布来自事务内的本人餐食查询；后续修改私人餐食不会自动更新已发布快照，重新发布必须明确同意。
9. 回退时保持鉴权/维护模式并保全邀请/空间/私人分区。不能删除用户记录、在旧代码上重放spaceIntent，或让旧空间内容自动出现在新成员关系中。
10. 最终云端双账号测试：非成员读写、错误空间版本、两人并发接受同一邀请、退出与写入竞争、丢回应后重试、撤回后旧请求和新伙伴隔离。全部留待统一验证。
