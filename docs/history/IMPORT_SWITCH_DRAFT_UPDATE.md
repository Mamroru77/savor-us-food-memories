# 导入搜索开关与草稿字段完整性

> 最新纠偏：原“云端商家搜索”只是腾讯分店定位，不是用户要求的商家页面资料获取。页面获取尚未接通；流程及公开页面复测见 [MERCHANT_PAGE_CORRECTION.md](MERCHANT_PAGE_CORRECTION.md)。

日期：2026-09-11。本次以此说明为准，取代旧版“搜索只能改代码开启”的操作说明。

## 自行开关云端商家搜索

Add → 万能导入面板新增“云端商家搜索”开关，不依赖是否已经生成候选。

- 本机首次使用默认关闭，开启前显示用量确认，确认后记住选择；退出重进、重新编译后读取本机偏好。
- 关闭时不调用 placeLookup，保留本地分类与原生选点；开启仅显示城市及搜索入口，**不会自动发请求**。生成候选、修改文本或打开开关均不自动搜索。
- 只有点击搜索按钮才调用云端。关闭开关会使已有搜索结果、进行中的结果及旧分店确认失效；已发出的云端请求无法靠前端开关撤销，也不能撤销已产生的用量。
- 偏好保存失败会显示错误，并回显实际保存状态。偏好存储独立于日记／草稿，不清理旧数据。
- 用户已确认后台 PLACE_LOOKUP_ENABLED 为 true。本次不修改后台环境变量，保持现有 Key、OPENID 白名单和开关配置。前端开关不能越过后端白名单、权限或额度限制。
- 仍保留 1.8 秒 HTTP 期限，不要求升级套餐。免费额度与云开发计费需在控制台管理；前端开关不是费用封顶器。

## 修复候选填入草稿不完整

此前店名、菜系、类别、来源、确认位置可投影到草稿，但平台评分／人均及地区线索未保留，地址线索展示不够明确。本次增加完整参考资料区并贯通存储。

| 候选信息 | 草稿／保存行为 |
|---|---|
| 店名、菜系、已选择餐饮类型 | 填入对应可编辑字段 |
| 原始类别、类别来源 | 保留并在参考区展示 |
| 分享平台／堂食外卖语义、支持的分享链接 | 保留；链接继续移除跟踪参数，不请求分享网页 |
| 分享地址与地区线索 | 保留为明确标注的参考，不伪装为已确认地理位置 |
| 平台评分，如 4.8 | 单独保留为 platformRating，不替换个人评分 |
| 平台人均，如 ¥172 | 单独保留为 platformAveragePriceCny，不替换本次实际花费 |
| 已经通过原生选点／腾讯搜索明确确认的位置 | 保留分店名称、地址、坐标、来源及搜索 POI ID |
| 未确认地址 | 不制造坐标；仍需核对实际分店才能保存 |
| 实际用餐日期、个人评分、实际花费、笔记和照片 | 导入会清空实际日期待填写，其他既有个人内容保持不变 |
| 尚未采纳的店名分类建议 | 不自动当成确定分类填入 |

原“人均消费”和“评分”标签改为“本次实际人均消费”和“你的评分”，避免与平台参考数据混淆。确认填入后关闭候选面板、提示成功并回到页面顶部查看参考资料。

修正不变店名的重复输入事件会清除类别／位置的问题；真实换店时清除旧参考资料，避免把旧店平台评分带到新店。

新增 importAddressHint、importAreaText、platformRating、platformAveragePriceCny 的中央适配／云端校验／更新白名单／离线覆盖／编辑恢复链路。参考资料可以经过本地重启、云端保存、刷新和编辑回读；服务端只接受限定长度、有效数值和受支持来源，不将它们当成权限或个人评分字段。

## 部署步骤

1. **部署更新后的 mealRecords，云端安装依赖。**旧函数可能忽略新增参考字段，必须先更新，避免保存后丢失。
2. 编译／发布新版客户端。首次使用的本机开关仍为关闭，用户自行确认开启即可；不再需要改 importPolicy.js 源码。
3. **placeLookup 本次无需重部署**，保持你已经确认有效的后端 true 配置和账号白名单即可。
4. 不清缓存、不重传照片、不改照片访问权限。上一版 Console 图片／样式修复与 Map D 保持不变。

## 验证与验收

本地及解压包验证：195/195 静态检查、158/158 mock 合约场景通过。本次新增 7 个场景：开关同意／取消与重载、不产生自动云调用、偏好失败与过期确认、关闭后的异步失效、完整候选到草稿重载、云端增改及编辑回读、Add 保存字段转发、同名输入和参考字段清理／边界等（部分组合在同一场景中）。原有“不得将平台评分冒充个人评分”的断言继续保留；原先“平台参考字段必须丢弃”的断言按本次需求改为分别保留和受限校验，没有跳过原回归。

本次没有实际操作用户后台或执行开发者工具／真机验收。已有用户截图仅证明上一版本 placeLookup 一次真实搜索返回 10 个候选。

上线后请验证：
- 关闭开关时只有原生选点；确认开启后出现搜索入口，未点击搜索不产生调用。退出重进仍记住选择。
- 用此次 1886 餐厅分享文案生成候选；填入后参考区有 4.8、¥172、分享地址、原始类别和来源；个人评分／实际花费没有被平台数值覆盖。
- 先核对分店再填入，已确认位置保持；未选位置不生成假坐标。
- 补实际日期、评分和花费后保存，重新编辑核对参考字段未丢失。
- 打开开关但后端停用／额度不足时可见错误，原生选点仍可使用。
