# 分享导入实施与联网调研

> 后续实现已增加类别提取与腾讯具体分店确认，见 [第一阶段补全说明](IMPORT_ENRICHMENT_UPDATE.md)。下文保留早期调研记录；线上权限与商家图片仍未验证。

日期：2026-09-11。基线：`298ff6e`。

## 1. 本次确认的范围

- 大众点评是堂食门店的主来源。
- 美团导入外卖店铺，其他外卖平台沿用“外卖”语义。
- 点评短链、美团堂食、失效链接、团购商品页不再作为首批必须补齐的样本。程序仍必须安全处理空输入、无法识别的文本与异常 URL，不等于扩展产品支持范围。

## 2. 联网调研：有没有公开导入示范？

**有相似产品的公开功能说明，但本次没有找到能核验的“第三方食记产品稳定导入大众点评／美团任意店铺链接”的直接示范。** 没找到不代表不存在。本轮没有安装或实操这些竞品，不能把产品自述称为独立成功率测试。

### Mapstr：官方说明，证据最直接

官方 FAQ 描述了完整操作路径：在文章／网页中点击分享，选择 Mapstr；应用分析内容、发现地点，再让用户添加到地图。FAQ 也说明 Instagram／TikTok 内容和 Google Maps 列表的导入方式。[1](https://en.mapstr.com/faq)

可借鉴：以用户主动分享为入口，先识别地点、再确认归档；不把“看到推荐”当作“已经吃过”。不能据此推断它支持大众点评或美团，也不能把原生 App 的系统分享扩展直接等同于我们的微信小程序能力。

### Beli：有媒体报道，不是本轮找到的官方教程

Food Network 于 2025-08-04 报道 Beli 能把 Instagram／TikTok 内容中的地点加入 Want to Try。[1](https://www.foodnetwork.com/fn-dish/news/beli-app-trend)

可借鉴：候选／想去与实际到访记录分离。该报道不证明点评、美团支持，也没有披露可复用的抓取接口。

### 美团：存在官方商家详情能力，但需要授权接入

外卖 OpenAPI 文档列出商家列表、商家菜品和商家详情能力，包括根据商家 POI_ID 查询详情；搜索取得的文档同时列出未审核调用的测试参数限制。[2](https://open.waimai.meituan.com/openapi_docs/index.html)

实际打开该文档时，页面跳转到美团技术服务合作中心的“快速接入”页，页面标注更新时间 2021-08-19，说明 OAuth 授权、签名及美团分配的 app_id 等认证参数。这是有授权前提的正式接入路径，不是匿名输入任意分享 URL 即可读取资料的公共解析服务。[2](https://open.waimai.meituan.com/openapi_docs/index.html)

原始开放平台接入指南还描述先联系合作业务方、申请应用、审核后取得凭据的流程；其中“暂时只面向企业”的描述仅能作为该文档内容，不应扩大解释为所有当前美团能力都不允许个人开发者接入。[1](http://open.meituan.com/docs/introduce/guide)

本项目当前没有取得上述接口权限，也未调用它们。样例跳转中的 `poi_id_str` 没有经验证可直接用作详情接口所需 ID；不能自行假定编码与映射关系。美团授权身份也不能与微信 CloudBase OPENID 混用。

### 腾讯位置服务与微信：门店确认仍需真实配置验收

腾讯官方 SDK 列有地点搜索、关键词提示、地址解析等能力，可以作为后续候选分店补全的正式基础；这并不意味着地址解析的坐标一定是具体分店。[1](https://lbs.qq.com/qqmap_wx_jssdk/qqmapwx.html)

微信官方配置文档要求申明使用的地理位置相关接口，并说明管理后台接口权限设置；这些仍须用当前小程序的真实后台与设备验证，不能只靠本地配置文件断言授权已通过。[2](https://developers.weixin.qq.com/miniprogram/en/dev/reference/configuration/app)

## 3. 本轮已实现：不依赖抓取的分享文案导入

路径：**Add → 万能导入 → 选择来源 → 粘贴完整分享 → 生成待确认候选 → 确认填入草稿**。

- `utils/shareImport.js` 为独立的本地解析模块，无网络、剪贴板监听、存储或定位副作用。
- 3 条点评样例可提取店名、地址线索、区域文字、菜系、平台数字评分及人均参考。
- 2 条美团样例提取店名／分店，归类外卖；缺失地址不补猜。
- 其他外卖平台提供通用文案入口，支持类似 `「店名」`／`【店名】` 的名称模板。不匹配时手动填写；**尚无其他平台真实样例或网页解析支持证据**。
- 支持本批分享中的 Markdown 链接及 `&amp;`。纯链接也可以进入候选，但缺少店名时必须手填。
- 每次一家，最多 6000 字符；多家文本明确拒绝，不混合名称与地址。
- 来源链接只保留本批已知点评门店／dpurl 分享路径的规范化 URL，去掉跟踪查询参数。其他链接不自动打开、抓取或写入来源字段。
- 候选明确标记“未经网页核验”；平台评分／人均只展示参考，不写入个人评分、双人评分或实际消费。

### 草稿与保存边界

- 填入前有确认：替换店名／菜系、清除旧位置和日期；保留现有备注、照片、个人评分与实际消费，明确提醒用户检查。
- 新草稿本来的未评分、空消费不会被平台值替换。
- 导入本身不创建餐食、不上传照片、不增加到访统计。用户仍需选择实际用餐日期、腾讯地图分店，并按 Save 才进入既有保存链路。
- 外卖仍需确认商家分店，不使用收货地址冒充门店；当前没有“无坐标外卖记录”保存分支，搜索不到实体门店时还不能完成保存，这是现有限制。
- 候选取消不改原草稿；确认期间草稿／候选发生变化会拒绝覆盖。
- 本机存储失败时不替换原草稿，不显示虚假成功。
- 保存、编辑、上传、定位进行中不允许导入覆盖草稿；进行中的编辑重试受保护。
- 可选字段 `diningMode`、`sourcePlatform`、`sourceUrl` 已接入中央适配器和服务端清洗，支持云端保存及再次编辑；原始整段文案和平台参考评分没有写入云端。它们是用户提供的来源说明，不是平台认证标记。

## 4. 一并修复：编辑缺失队列不再代表成功

旧路径可能在 `editOperationId` 已不在本地 outbox 时直接认定完成并清空草稿。现在在清空前，通过 owner-scoped get 读取该记录的服务端 `operationReceipts`，核验对应操作及版本。

- 存在可核验回执：完成身份匹配的草稿清理。
- 队列不存在但无回执、读取失败、记录身份不符：保持草稿并显式失败，不生成新的重复编辑请求。
- 未完成确认的编辑不能被另一条编辑或输入覆盖。
- 回执仅保留最近 32 次操作；历史无回执或已被淘汰的操作可能持续无法自动确认。**此批实现的是保守阻断，不是完整的历史恢复界面或无限期操作台账**；后续 S1 仍需补齐可确认的恢复协议。

## 5. 验证结果

**191/191 静态检查、108/108 mock 场景通过**，较前版增加 16 个场景；原有测试未禁用。

新增覆盖：5 条真实文案、Markdown／HTML 实体、多门店／超长／空输入、来源选择、裸链接与通用外卖模板、安全 URL、个人字段隔离、确认／取消／并发草稿保护、存储失败、来源字段云端往返、缺失队列的回执核验与失败保留。

UI 改动仅为既有 Add 页的局部导入控件及来源提示。UI 基线批准清单记录了对应两个 WXML/WXSS 文件及本次用户授权原因，未取消基线测试。没有改其他 Tab、Me 高度、主题体系或页面结构。

**未执行：微信开发者工具／真机、线上 CloudBase 部署、安全规则实测、竞品 App 实操。** 5 条文案测试通过不是 5 条网页抓取成功。

## 6. 部署及后续

1. 使用最新包，重新部署 `mealRecords`（云端安装依赖），再编译前端；不清缓存、草稿、数据库或 outbox。
2. 在真机 Add 中逐条粘贴本批样例，核对中文／英文、候选、取消、日期、腾讯选点、保存及重启后的来源字段。
3. 优先推进 S1 身份／账号缓存分区、旧回执恢复和私有媒体授权；目前这些基础仍未全部实现，未开放情侣共享。
4. 共享 Wishlist、情侣绑定、各自评分、OCR、正式授权的网页补全仍未完成。本轮只是提前落地不依赖共享权限的本地导入部分，不能称为 S1–S4 全部完成。
