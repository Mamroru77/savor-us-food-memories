# Tab 点击先抽动：移除叠加的按压几何动效

日期：2026-09-13；基线：d4e9310。

**源码与测试通过，真机复测待执行。** 本轮仅修改TabBar样式，不改导航或形变引擎，不宣称全部历史回跳已闭环。

## 已确认的额外动效

用户指出当前表现为“点击其他Tab，图标先抽动一下，随后才开始过渡”。检查发现SVG形变之外还叠加了按压样式：

- 普通Tab：`.tab-press .tab-icon-wrap { transform: scale(0.88); }`，容器另有220ms transform过渡。
- Add：按压时`scale(0.9) translateY(-4rpx)`，同时更换阴影与光圈范围，按钮有180ms transform过渡。
- WXML在按下时立即触发hover-class，并在松手后保留100/120ms。

所以点击确实会先启动一段独立的缩放/位移；恢复常态或切页后再衔接480ms SVG形变。这与用户描述吻合，但最终仍须在真机确认是否消除了当前抽动。

## 改动

生产文件只有：`miniprogram/custom-tab-bar/index.wxss`。

- 删除普通Tab的按压缩放，保留opacity 0.7反馈。
- 删除Add的按压缩放、上移及按压阴影变化，改为opacity 0.85反馈。
- 删除容器transform过渡和已经被后方规则覆盖的旧选中抬升/pop定义。
- 正常与选中状态的尺寸、位置、配色、玻璃材料及阴影不变。
- 保留hover-class、触控区域及Quiet禁用过渡的行为。

未修改任何JS、WXML、官方SVG、480ms预算、动画首帧加载保护、导航、页面接入、Map或业务代码。

## 其他检查及边界

另外将五组图标双向的官方静态SVG与形变引擎t=0首帧在本地栅格化对照。发现轻微的采样/绘制差异，但没有据此认定或修改引擎。该检查不是微信截图，也不能排除原生图层切换或跨页状态问题。

本轮先移除确定存在的按压几何动效，避免同时改动导航、状态机与图形算法。若仍抽动，再根据修正版录屏区分位置/缩放变化和真正的端点回跳。

## 验证

- 229/229静态、21/21 Tab接入、233/233 mock、44/44交接、44/44 UI。
- 新增2项UI回归：按压不得缩放/位移/pop或改变Add几何阴影；按压反馈、130rpx按钮及Quiet保护继续存在。
- 把d4e9310旧CSS代入新UI测试，会在按压几何检查处失败；新CSS通过。
- 实际ZIP解压副本同样完整验证后交付。

## 真机复测

不退出小程序，依次点击Home、Map、Add、Us、Me，再往返一次。观察点击瞬间是否仍先缩小、上移或弹回，然后才形变。

若仍有问题，请提供这一版原始录屏。无需为此恢复owner锁、增加等待或改变480ms。原生导航、完整形变与当前抽动分别验收，不以代码测试代替实际画面。
