# 回退旧抽屉，按参考图重做地图标记展开

> 普通 view/image 气泡兼容方案已被后续修复取代，现使用 cover 组件和定时分步展开，见 [MAP_CALLOUT_COMPAT_UPDATE.md](MAP_CALLOUT_COMPAT_UPDATE.md)。

日期：2026-09-11。本说明取代上一版 MAP_UPWARD_DRAWER_UPDATE.md 中的下方绿色按钮和有底色抽屉设计。万能导入仍暂停，本次不修改其代码或配置。

## 按最新参考图的变化

- 收起：只显示本组第一个照片快印标记，上方为白色圆形上箭头；不再显示侧面数字气泡或下方绿色数量按钮。
- 展开：其他照片快印从首个位置依次向上展开，先出现靠近根标记的项目；白色箭头向整列顶部移动，变为下箭头。
- 完成：图标垂直排列，无抽屉底色、无卡片格子、无透明占位块；组内主标记及子标记采用约 80×90px 的一致视觉尺度。选中状态仍反映真实选择，不因参考图中的勾号而将所有餐厅标成选中。
- 点击子标记更新底卡，点击第一个真实标记可重新选择第一家，箭头控制收起。组内根餐厅不被子项替换；没有改动真实经纬度。
- 保留底卡、方形照片、Map D 不重复店名、Me 布局和私人图片权限。
- Quiet／减少动态效果下直接展示终态，不播放展开动画。收起立即复位。
- 超过 3 个额外标记仍分页，避免无限长列；参考图中的 4 家餐厅可在同一页展示。小屏或根标记接近顶部时的遮挡情况需设备验收。

## 修复本次 Console 暴露的问题

1. **MarkerStyle.width / height 无效**：此前地图动画用 `markers[i].width` 等深层 setData 补丁更新原生 map。本轮改为完整 markers 数组提交，每项保持完整标记对象；初始及动画尺寸均为正有限整数，照片更新也不再单独提交 markers 深层路径。新增测试检查整个更新过程中传给桥接层的数据形状。
2. **cover-view 样式／同层渲染提示**：新实现使用原生 map callout 插槽下的同层 view/image，不再使用 cover-view/cover-image，不再设置不受支持的 overflow:visible。
3. 上一版对模拟器不支持 moveToLocation 的检测及真机失败回退继续保留，不能把这项必要修复一起回退掉。

这说明修复了已识别的代码风险，不代表仅凭截图已证明 SDK 内部错误的唯一根因。仍需用本次包在微信开发者工具确认错误不再出现。

## 验证与更新

- 195/195 静态检查、169/169 mock 合约场景通过；唯一最新 ZIP 解压后同样通过。
- 新增覆盖完整原生 marker 数据提交、正整数尺寸、照片刷新与动画并发、参考图布局结构、依次向上动画、Quiet 模式和重新选择首家。
- 原有私有图片异步防串图、分组坐标不变、分页完整性、地图失败回退等回归保留。旧版“下方箭头／数字按钮”断言已按用户最新参考图改写，非跳过测试。
- 本次未运行微信开发者工具或真机，不能将 mock 当作同层 callout 实际定位、层级、CSS 动画和触摸验收。
- 仅客户端更新，不部署云函数、不清缓存、不重传照片。

## 建议优先验收

1. 完整编译后进入 Map，观察 Console 是否仍出现 MarkerStyle 的 width/height 错误；不要只依赖热重载结果。
2. 两个邻近餐厅：白色箭头在主标记上方，展开后第二个图标在上方，无底色列表和重复数量气泡。
3. 四个邻近餐厅：子标记依次向上展开，最后箭头在整列顶部；选中卡片、收起、重新展开均可操作。
4. 开启 Quiet 后直接展开终态；快速切换、筛选及离开地图无旧图片写回。
5. 在真机核对同层 view/image callout 是否与真实标记对齐；若仍有差异，依据真实截图调整渲染，不移动商家真实坐标。
