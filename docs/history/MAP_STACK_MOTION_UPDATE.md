# 统一地图标记布局与双向过渡

日期：2026-09-11。用户指出上一版与参考图差异大，且展开／收起没有过渡。本轮替代“100ms 增加一项／瞬间收起”的实现，万能导入不动。

## 结构问题与本次修改

之前首个图标是原生 marker，子图标是另一个 callout 布局里的图片，两套高度和间距规则容易出现断层；逐项增加 DOM 也不等于位移过渡。

本轮统一为：多餐厅组的所有可见图标（包括第一家）都在同一个原生 customCallout 中布局。原地图 marker 只保留真实经纬度上的 1×1 正数透明锚点，不显示重复底图。单个餐厅仍使用原来的原生照片标记。

- 首项和子项统一 80×89px；静止时相邻图标顶部相距 101px（12px 间隙）。
- 箭头位于列顶，根图标底部始终对齐同一锚点；展开时列向上增高，收起时向下回到根图标。
- 展开和收起各约 320ms，以约 20ms 间隔更新 top、容器高度和 opacity。子项有轻微错峰和平滑插值；不是新增一项再等待的阶梯显示。
- 收起完成前保留子项，等待它们回到根处并淡出后才移除。快速反向点击从当前进度反向，不先跳回端点。
- cover-view 基础数值样式驱动，不依赖不支持的 CSS transform/transition/animation。
- Quiet 模式直接显示终态，翻页直接切换；每页最多 3 个子项，分页仅在需要时出现。
- 所有图标仍用各自实际照片和原有选中／未选中框；无照片时显示占位图，不伪造商家照片或把所有项目标成选中。
- 不改真实坐标、不写食记；动画帧只更新 UI，不增加云图片解析或网络请求。照片在渲染准备阶段复用既有渲染器及缓存。

## 保留的兼容与安全边界

原生 marker 仍整数组更新，宽高为正整数；开发者工具跳过不支持的 moveToLocation，真机失败回退保持。过滤、缩放、回到概览、退出地图会取消动画；旧照片异步结果不得写入新选择。私人图片权限、底卡、Map D 和 Me 均不修改。

透明锚点不是假坐标：它仍是该组根餐厅的真实坐标，子项只在屏幕空间排布。打开第三方地图时仍使用被选餐厅自己的坐标。

## 验证结果及边界

196/196 静态检查、173/173 mock 合约场景通过；唯一 ZIP 解压后同样通过。

验证包括：展开与收起中间帧位置／透明度、保持根图标底部不动、终态等间距、快速反向位置连续、动画期间无额外图片请求、退出取消、Quiet、分组／分页、完整 marker 数值类型等。旧逐项出现测试已替换为真正的双向中间帧断言，没有删除回归入口。

**尚未在微信开发者工具或真机验证帧率、原生气泡测量和截图相似度。**数值中间帧通过不等于设备视觉验收通过。若地图根点接近顶部或分组较大，仍需检查页头遮挡及分页触摸。

## 更新

仅客户端更新，正常完整编译；无需重新部署云函数、改 Key、清缓存或重传照片。

请重点检查：
1. 收起时只显示首个图标及上方白色圆箭头；没有额外原生底图。
2. 展开能看到子项从根位置向上移动，而非瞬间跳到最终位置。
3. 收起能看到反向回落，而非直接消失。
4. 首项与子项等尺寸、静止等间距，选择子项更新底卡，实际照片与占位图保持各自归属。
5. 快速连续开关、切换分组和退出重进，不残留半展开状态。
