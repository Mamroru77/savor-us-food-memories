# S1 部署与回退（待授权）

当前环境没有操作云端、没有创建集合、没有修改权限、没有推送仓库。

## 部署前门槛

1. 备份当前本机工程、云函数版本，以及用户自己授权保全的本机数据。不要上传真实记录到公开报告。
2. 核对既有真实云环境 `cloud1-d9gqm52id66c0bcda` 和 AppID，不擅自替换环境或密钥。优先免费配置，不升级套餐。
3. 管理员授权后创建 **savor_accounts** 集合，客户端直接读/写均禁止，例如 `{"read":false,"write":false}`；由云函数服务端 SDK 访问。
4. 保持 dining_records 客户端禁止直读/直写；保留私人存储“仅创建者可读写”或更严格等价规则。无需清空原集合、复制示例、公开媒体或建立 couples 集合。
5. 对 account 云函数执行正常 Node 依赖安装/上传部署（wx-server-sdk ~2.4.0，与现有项目一致）；同时准备更新后的 mealRecords。新的 profile/couple/assets 服务未在本批实现，不部署这些不存在的接口。

## 协同发布

- 必须协调 `account`、`mealRecords`、小程序客户端三个版本，先部署服务端再测试新客户端，最后才发布小程序。
- 先 account 及账号集合，后受保护 mealRecords。握手 `identityHandshake` 为只读能力检查，不修改餐食。
- 新 mealRecords **拒绝缺少身份协议的旧客户端**。正式切换前需确认用户范围/维护窗口；不能静默部署到多人在用的环境而不告知旧版会暂时无法同步。
- 新客户端在 account 或受保护 mealRecords 缺失时保持锁定，不能放宽代码以跳过握手。
- 每次 bootstrap 至少调用 account + mealRecords；写餐食前额外握手，增加云调用量。没有“零用量保证”。
- 完成真实两账号权限验证后才可发布；不要把模拟器云函数成功当成两账号安全验收。

## 回退边界

- 不要简单 `git reset --hard` 后发布旧客户端！旧客户端仍读取未归属原始旧键，回到旧自动同步行为可能重放未知操作。
- 优先在本轮客户端保持身份锁定/维护提示，暂停业务提交，保全三类本机键和云端映射。
- 禁止删除/重建 savor_accounts 产品 ID 映射，禁止删除已创建私人分区或原始 diary/draft 键。
- 回退前导出需保全的当前私人分区与隔离区备份。新分区内容不会自动写回旧格式；没有安全的自动 downgrade。
- 如必须回退服务端，需要同时停止客户端业务入口并规划数据恢复；保留身份拒绝机制，不能把 owner-only 的旧版本当作与新身份协议等价。
- 修复范围之外不改 Map 样式、Tab 动画，不开启共享，不更改腾讯商家配置。

## 不可逆操作须另行确认

创建/修改真实集合、部署云函数、调整数据库或存储规则、清理任何本地/云文件、Git 提交/推送和正式发布，均需要用户明确授权。本文不是执行授权。
