# S3-01：共享媒体授权与生命周期候选

2026-09-14。遵循“开发优先，最后统一Codex/真机验证”的执行策略。本批没有云端部署、权限调整、真实文件删除或Git推送。

## 已实现

### 上传与登记
- 新增 media 云函数及 savor_media_assets 登记集合的代码。
- 主人对当前活跃空间内本人发布的餐食明确选择照片，prepare在事务中保留槽位（每餐最多9张）。
- 客户端保存本人分区内 mediaIntent（文件路径、操作身份、原空间版本和上传阶段），不把base64大块或共享图片塞入私人日记。
- 服务器接收照片字节，检查JPEG/PNG格式、大小/尺寸、PNG CRC，去除JPEG APP/COM与PNG非渲染辅助信息后生成共享副本。
- 只允许共享副本最长512KiB、最长边4096像素、最多16777216像素。客户端先尝试压缩；不支持SVG、HTML或任意data URL。
- 共享副本由服务器上传至专属路径并登记真实返回的fileID，绝不接受客户端指定fileID作为资产归属依据。
- 内容摘要固定后不能替换同一上传身份的图像。上传登记后，finalize再次检查当前关系及发布状态才激活。

### 授权读取与撤回
- 主人和当前另一成员按实际服务端身份、活跃空间和成员版本访问。
- 对外只返回资产描述符及已授权图像字节，不暴露云文件ID或签名URL。
- 文件下载后再次验证成员关系、餐食发布状态及资产状态，防止下载期间撤回仍继续授权。
- 客户端使用独立短期显示目录，15秒内的显示租约从请求开始计时；隐藏/身份变化/过期时清理。
- 原私人照片目录和未管理文件不进入此清理范围；启动仅清扫本功能规定命名的旧临时显示文件。
- 主人可撤回照片；取消未知上传结果会留下持久化取消记录，避免迟到prepare重新激活。
- 餐食撤回时同时撤回其已登记照片。重新共享文字不会自动恢复已撤回照片。
- 退出空间后，服务端后续请求被拒绝；已看到/下载/截图的信息不能被技术上追溯收回。

### 失败恢复和回收
- prepare/upload/finalize支持同一操作身份重试。上传回执丢失后，可查本人资产状态，不必重新读取已丢失的本机源文件。
- A的待确认上传不会成为B的上传；陈旧读取/清理回调不会删除新账号刚建立的显示缓存。
- 撤回或清理期间到达的上传结果仍登记来源，保持不可读，不把物理孤儿文件遗漏在登记之外。
- 新增管理员候选分页扫描、只读cleanupCheck和显式purge接口。
- purge默认关闭：需要真实管理员OPENID白名单、明确保留期和开关；客户端伪造admin字段无效。
- 先冻结登记再删除；删除失败保留purging供恢复；迟到上传登记通过fileRevision阻止旧删除完成回调误记为彻底删除。
- 记录实际管理员产品ID、采用的保留天数和请求时间。不会清理未登记的旧私人照片。

## 本批限制（非隐藏能力）

1. 当前主动共享照片由设备选择器选择并上传新副本；不自动把旧cloud://文件发布给另一成员。
2. JPEG/PNG和512KiB上限是当前代码约束；最终需核验真实CloudBase调用包大小、平台压缩效果和微信图片解码。
3. 去除文件头辅助信息不等于匿名化画面：照片本身、可见文字、水印等不会被自动抹除。
4. 已授权客户端或接收者保存的副本无法被撤回；15秒短期显示机制也不是DRM承诺。
5. 回收代码就绪，不代表保留期已批准或删除任务已启用。当前没有设置真实管理员/保留期/开关。
6. 旧私人本机照片的全局GC仍保持保留策略；本批只处理本功能登记的共享副本及其受管临时显示文件。

## 自动验证

- `npm run verify:media`：30/30媒体backend/client场景。
- `npm run verify:all`：继续包含S1身份22、S2空间26、既有业务233等回归。
- JPEG/PNG清理后的测试图片另外通过Pillow实际解码检查；不是微信原生图片验收。
- 全量执行结果见 CURRENT_TEST_RESULTS.txt。真实CloudBase事务、存储ACL、图片限制和真机行为统一留到最终验证。
