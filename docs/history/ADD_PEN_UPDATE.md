# Add: approved Pen / PenLine

User confirmed the interactive SVG proposal. Add now uses exact official Lucide Pen (unselected) and PenLine (selected), including both static icon aliases and the real morph endpoints. The lab's production-pair example matches.

Preserved: 66rpx Add icon, 130rpx round button, themes, 1.75 rounded strokes, 380ms smoothstep, five routes, SVG image-layer renderer, bounded pending frames, cached replay, Quiet and fallback. No map, layout, cloud or business changes.

Added two reviewed SVG originals with source hashes. Registry: 47 canonical shapes plus 26 compatibility aliases; generated from the single SVG source set. Plus remains used for ordinary add controls; SquarePlus remains a supported historical/compatibility shape, not the active Add Tab endpoint.

Regression checks cover the exact official Pen/PenLine paths, all production pairs, finite intermediates, SVG reverse/re-entry/Quiet and existing native-layer guards. Native visual/performance and residual ghosting acceptance remain NOT EXECUTED. No cloud deployment required.

Current visual reference: `docs/design/add-tab-preview.html`. Earlier full-catalog previews document the previously approved Add design, not the current endpoint pair.
