# UI Quality Audit · P0 / P1 / P2 最小修复

日期：2026-09-12  
修复基线：`282c783`（已批准的餐具 Add / 全部 Tab 480ms）  
授权：用户要求“以优先级按最小修复方向修复 P0 P1 P2”。

## 交付范围

已实施确定问题的 UI 层修复和低风险布局防护；没有重设计、云部署、业务模型/API 变更或新图片/图标替换。

**不能把本次交付理解为全部原生验收已完成。** 以下状态表明确保留 F02 地图竖向命中区、F16/F17 设备验收和 F18 分隔线的未闭环部分。P3 不在本轮实施范围。

## 按原审计编号对照

| 编号 | 优先级 | 实施结果 | 状态边界 |
|---|---|---|---|
| F01 | P0 | 移除 Library 内层 675rpx 高度上限及隐藏裁剪，外层已测量 scroll-view 继续负责滚动 | 0/1/5/20/100 条、筛选/无结果逻辑通过；原生最后一行可达性待测 |
| F02 | P1 | 关闭、清除、评分、点赞、标签、Toast、普通 Map 控件等扩大到 88rpx 级别；小图形保持原尺寸 | Map 原生堆叠仅将透明命中区横向扩大至 44px，纵向仍保持原值，避免未经实测改动叠放命中关系；该部分未完全闭环 |
| F03 | P1 | 27 个原生表单控件补本地化可访问名称；input/textarea 补 focus/blur、统一占位；Add 按字段显示首错并滚动定位；导入和保存错误分区；资料/个性化姓名错误可见 | 不改验证条件；读屏、键盘实机待测 |
| F04 | P1 | 浅色 helper/placeholder 复用现有 muted；保留暗色覆盖 | 固定颜色样本计算通过，玻璃/照片实际像素对比度待测 |
| F05 | P1 | uploading/saving 均用原有 loader-circle，闲置仍为 NotebookPen；忙碌语义及输入禁用；Quiet 停止旋转 | saveLock、保存顺序、草稿保留不变 |
| F06 | P1 | 移除通用 Toast 成功勾号，保留文本/关闭和 message-only 接口 | 不用文案猜测成功失败，不新增消息类型 |
| F07 | P1 | Home/Us/Me 只由末尾 spacer 保留完整 Tab clearance，页面保留 16/20rpx 内容间距 | Map/Sheet 独立空间策略不改；Add 原底部空间不扩大本轮范围 |
| F08 | P1 | Us 文案 right 100rpx，明确避让右下 88rpx 点赞区域及 12rpx 间距 | 卡片尺寸和照片材质保留；长文案实机待测 |
| F09 | P2 | 详情缩略图改用原生横向 scroll-view，90rpx 方形不收缩；显式计算轨道宽度，选中项滚入可视区 | 支持既有 1–9 张，不改顺序/数量/预览数据 |
| F10 | P2 | 危险按钮 border-box、88rpx 最小外高，与 secondary 一致 | 保留颜色和不等宽优先级 |
| F11 | P2 | 普通自定义操作补轻量 pressed；分类/Library/详情等补选择语义；分类与 Library 增加下划线，未新增图形 | **复核纠正：主题与菜系本来已有 check 和 aria-pressed，保留而非重复添加** |
| F12 | P2 | 表单字号/行高、caption 采用语义角色；Us 450 字重改为已有 500 | 不机械统一所有字号和图标尺寸；保留中文、Tab、Map 几何特例 |
| F13 | P2 | 增加 5 个实际使用的表单/文字 token，初值尽量与现有视觉一致 | 已有 content-card 色/阴影 token 继续复用，不另造材料系统 |
| F14 | P2 | metrics 可显式刷新；五页 show/resize 更新头部；Sheet 打开/resize 更新 sheetTop 并重新测量，保留解绑和 detached 防护 | 事件驱动，无轮询；旋转/分屏实机待测 |
| F15 | P2 | 内容图补一次性视图回退，保留原图数据/预览；同源错误不循环，后续进入可重试 | 不修改 store、cloud://、下载/清理/签名 URL 生命周期；Memory Row 原回退保持 |
| F16 | P2 | Sheet 标题命名、输入焦点、textarea cursor-spacing、姓名首错滚动定位；保留原容器测量和退出行为 | **部分完成：13 类 Sheet 的键盘、读屏和到底流程仍需原生验收；没有伪造 Web focus-trap** |
| F17 | P2 | Add 副标题允许换行；日期/评分局部流式布局；Sheet 标题、元数据和操作长文本保护 | **部分完成：七档宽度、长中英文及系统大字的真实截图压力测试未执行** |
| F18 | P2 | Memory Row 及其三处调用者已复查，增加原样保护检查 | **未改：组件内部 last-child 在原生中是否误去掉全部分隔线仍未复现。不能为修风险直接给已批准列表加分隔线** |

## 几个重要的最小化处理

### 小控件扩大的是命中区，不是图标

- Sheet / Add 关闭按钮、Map 筛选和折叠按钮等使用原尺寸的内层材料/图形，外层接收点击。
- Toast 利用已有 padding 内的空间扩展关闭命中区，不额外增高可见 Toast。
- **Add 小照片的删除按钮移到每张缩略图下方的独立 88rpx 操作区**：不能让扩大后的删除热区覆盖大部分照片、导致原本想预览却误删除。照片本身仍为 108rpx，条带因此局部增高。
- Us 文案避让按照实际新命中宽度计算，不只考虑图形尺寸。
- Map 原生堆叠图形仍由 native callout 持有；没有增加可见普通图层，没有改变堆叠坐标、垂直间距或 320ms 动效。

### 仅新增 UI 状态

`utils/uiFeedback.js` 只处理焦点与图片失败状态，不依赖 store 或云服务。`imageErrors`、`focusedField`、`fieldErrors`、`sheetScrollTarget` 等只留在视图实例中，不写入日记/草稿，不成为业务字段。

### 确认不变

- 生产五 Tab **480ms**；餐具 Add 两端点、130rpx 圆形、66rpx glyph、官方 1.75 描边。
- SVG 首帧 load+commit 门控、缓存重播、反向切换、Quiet、超时降级。
- Map 原生渲染架构、照片 stamp、图钉定位和卡片/堆叠 **320ms**。
- 已批准卡片材质、Nocturne、中文标题层级、用户内容。
- 云契约、权限、分享范围、地理字段、费用/评分、存储 key、导入业务、草稿和照片生命周期。
- 保存防重与错误保留草稿；没有关闭或跳过原有校验。

## 测试与验证

命令：

```sh
npm run verify:all
# 或仅本次新增回归
npm run verify:ui
```

新增 `tools/verify-ui-quality.cjs`：**42 个源代码/隔离逻辑回归**，覆盖 Library 0–100 条、筛选、缩略图、WXML 平衡与重复属性、27 个字段名称、焦点竞态、五类 Add 首错、保存 guard、消息语义、底部占位、图片错误不循环/不改原数据、metrics 刷新、地图横向命中区不重叠、Tab 保护等。

原有验证全部保留。仅按本轮批准的真实变化调整：

1. 图标模板计数 **75 → 73**：恰好移除保存中和通用 Toast 的两个误导性成功勾号；仍为 48 个官方 SVG / 28 个别名。
2. Sheet 已包含横向子 scroll-view，旧“第一个关闭标签”断言改为核对**外层**关闭位置，仍验证尾部 spacer 在外层滚动区内；新增标签平衡和缩略图滚动契约检查。
3. `ui-approved-updates.json` 只更新实际变动的、用户本轮授权的视觉文件，不重置基线。

最终验证结果写入 `reports/latest-validation.log` 和 `reports/ui-quality-fix-validation.json`。源码包与 ZIP 解压副本均运行完整套件；未把 mock 当作真实微信或 CloudBase 验收。

## 原生验收清单（全部 NOT_EXECUTED）

- 375 / 390 / 430：浅暗 × 简中/英文/跟随系统，长名称、最大文字、键盘、胶囊与安全区。
- 768 / 1024 / 1280 / 1440：仅在原生微信实际支持的窗口/分屏模式检查；不是新增桌面 Web 产品。
- Library 5/20/100 条滚到底及筛选后最后一项。
- Add 0/1/9 张、照片预览与删除独立命中、评分、首错定位、保存慢网/失败/重复点击。
- Sheet 全 13 类：进入、输入、首错、键盘弹起、到底、关闭、再进入。
- Map 搜索/清除/筛选、卡片折叠、堆叠展开/翻页、拖动/缩放、原生图形与透明点击区对齐。
- 图片失败与恢复后再次进入；Toast 成功/失败/长文案。
- Memory Row 分隔线：Home 最近列表、Library、weekly 三处实机对照，决定是否需要后续补丁。
- VoiceOver / TalkBack 在微信中的名称、状态、顺序和可达性；平台不支持的能力需如实记录。
- 五 Tab 首次、缓存、快速反向、后台恢复、Quiet；实际 480ms 可见节奏和图层残影。

**不包含 P3 宽屏产品重构或动效重新设计。无需重置环境或部署云函数。**
