# 开发进度与 Console 修复

> 最新纠偏：原“云端商家搜索”只是腾讯分店定位，不是用户要求的商家页面资料获取。页面获取尚未接通；流程及公开页面复测见 [MERCHANT_PAGE_CORRECTION.md](MERCHANT_PAGE_CORRECTION.md)。

> 后续更新：现已支持本机可自行开关搜索和完整草稿参考字段，见 [IMPORT_SWITCH_DRAFT_UPDATE.md](IMPORT_SWITCH_DRAFT_UPDATE.md)。后端保持用户已确认的开启配置；旧文中的固定客户端开关说明已被替代。

更新日期：2026-09-11。本报告区分代码完成、本地验证和真实环境证据，不以总体百分比掩盖未验收项。

## 本次修复

### 食记列表云图片

`components/memory-row` 不再直接把 cloud:// 文件 ID 交给图片渲染器。复用中央 `cloudRecords.resolvePhotoUrls` 获取临时 HTTPS 展示地址；原始食记仍保留 cloud:// ID，不写入临时链接，不改数据库或存储读权限。

- 请求期间先显示现有本地兜底图；失败、拒绝、未解析 ID 均不传给渲染器。
- 图片源变化、组件卸载后，旧异步结果不能覆盖当前图片。
- 同一组件中同一源的语言变化／挂载不会重复申请地址。
- HTTPS 图片加载失败时最多重新解析一次，避免过期地址造成持续失败，也避免无限重试用量。
- 本地图片不申请云地址；清空记录会清空文字并复位图片。
- 不新增全局私有图片缓存，不把私人照片开放给其他用户。原有云文件解析和图片读取仍可能计入平台用量；“免费优先”不是整应用零计费保证。

### Sheet 样式警告

把 `.theme-option s-icon` 改为 `.theme-option-check`，对应图标添加 class。修复组件 WXSS 标签选择器限制，不改 Me 尺寸、主题设计、Sheet 滚动或 Map 布局。

### 不盲目处理的工具提示

`App.onLaunch took 86ms` 是性能提示；字体就绪、LazyCodeLoading 是正常日志；WAService/WAWebview 的 preload 提示暂未体现业务故障。没有为消除这些日志修改开发者工具内部资源或删除必要初始化。

## 当前进度

| 模块 | 代码现状 | 验收状态／后续 |
|---|---|---|
| 原生五 Tab、best-ui、Nocturne、个性化、中英与系统语言 | 已实现并持续保留 | 全页面真机排版、键盘和 Sheet 滚动仍需完整验收 |
| 食记新增／编辑／删除、收藏等状态、云同步与本地草稿 | 已接入 CloudBase 与中央服务，含 owner 校验、revision、待同步队列及兼容数据 | 本地回归通过；两账号隔离、并发、断网／杀进程和照片异常需系统实测 |
| 日期、菜系、人均、菜品、照片、未评分、真实统计 | 已实现相关基础能力；兼容旧双评分但不伪造双方评分 | 跨日与旧数据实际回读验收待补 |
| 通用导入 | 分享文本本地提取、类别来源、菜系与餐饮类型、待采纳建议、分店确认后进草稿 | 五个既有样本已用于回归；完整真机导入→确认→保存→重新打开待验收 |
| 腾讯具体分店搜索 | 已实现固定城市搜索、私有白名单、输入／响应限制、双开关和 1.8 秒 HTTP 期限 | **用户提供的开发者工具截图已显示 success:true、provider:tencent、pois:Array(10)**；本次真实测试通过，但不证明长期稳定、真机或全链路成功 |
| 免费优先 | 交付客户端搜索开关仍为 false，默认原生地图选点；后端环境缺省关闭 | 用户测试时云端曾设为 true；是否已经关闭无法从本地确认。建议测完设回 false，不升级套餐 |
| Map D | 标记不重复店名；保留聚合数量、底卡／收起条、方图、选择动画和居中 | 本次未改 Map；真机聚合／动画及照片显示仍需验收 |
| 列表云图片与 Sheet 选择器 | 本次已修复代码和新增回归 | **尚未用用户设备验证本次修复** |
| 大众点评／美团远程商家资料、商家图片 | 尚无已验证的授权稳定数据源；图片延后 | 不承诺任意分享链接可获得完整资料，不抓取或伪造商家照片 |
| 独立私人账号＋明确共享的双人空间 | 现有 owner 身份保护已实现；完整绑定、邀请、同意、撤回及共享生命周期未完成 | 继续 S1 权限／两账号验收后推进；shared 字段不等于共享授权 |
| 双人共同 Wishlist | 已明确产品方向，未完成运行时业务 | 待双人身份与权限基础落实；不伪装为已吃过的食记 |
| 云照片长期回收、解绑与保留策略 | 部分基础保护存在，完整策略和安全回收未完成 | 保留期与删除／撤回规则仍需明确；不可直接批量删云文件 |

年报／里程碑、真实订阅提醒、反馈投递、云端批量备份导入等扩展也不在当前已完成范围。

## 验证证据

- 本次源码：195/195 静态检查、151/151 mock 合约场景通过。
- 新增 4 个场景覆盖：临时解析／不持久化／去重、旧结果与卸载防护、失败与有界重试、本地资源与选择器。
- 唯一最新版 ZIP 解压后执行同一全量验证通过。
- 用户真实环境证据：腾讯接口先返回 121；分配配额后返回 10 个候选。没有将敏感 Key 或 OPENID 写入本报告。
- 未执行本次修复的开发者工具、真机、真实私有图片解析或两账号验收。

## 本次更新方式与下一步

此次修复为客户端更新，**不需要重新部署云函数**。替换为最新版项目后正常编译；无需清空数据、重建数据库、重传图片或开放存储权限。

优先顺序：
1. 打开原先报错的食记列表，确认有权限的照片正常显示，不再出现组件路径拼接 cloud:// 的错误；主题页面不再提示对应标签选择器警告。
2. 切换语言、快速换列表、离开再进入；断网或无权限图片应兜底，不串图、不持续重试。若仍报错，提供脱敏错误码，不要公开临时签名 URL。
3. 做一次完整导入→原生选点／可选搜索确认→草稿→填写实际日期→保存→重新打开，核对类别和分店坐标。
4. 补齐两账号、两设备、离线恢复及云权限验收，再推进真正的双人共享空间与共同 Wishlist。

安全提醒：此前截图中的地图 Key 已暴露，是否已重置无法从本地确认；若尚未更换，应使旧 Key 失效并更新服务端变量。不要把 Key、完整 OPENID 或私人图片临时地址发到公开截图中。
