# 商家页面资料获取：功能纠偏与公开页面验证

日期：2026-09-11。用户明确需要从分享链接对应商家页面取得店名、分类／风格、商家图片，而非腾讯地点搜索。用户确认没有授权接口，本轮只验证公开页面，不绕过登录或风控。

## 本轮交付

1. Add 文案入口改为“整理分享文案”“整理文案，生成候选”，明确这是本地文本解析。
2. 独立展示“商家页面资料”，明确“尚未接通商家页面资料获取”。没有假装可工作的开关或获取按钮，也不让用户为不可用功能消耗云额度。
3. 原误标的“云端商家搜索”改为候选下方的“腾讯地图分店定位（可选）”，开关名、确认弹窗、帮助与搜索按钮都明确是定位，不抓取商家页面或图片。
4. 不沿用旧误导标签下的同意状态。新本机偏好键为 savor-tencent-location-search-v1，首次关闭，需重新明确确认后开启；不清除日记、草稿或旧设置，不修改后台环境变量。用户已确认后台开启，该后台配置仍保持。
5. 腾讯定位错误只显示在定位区域，不再出现在文案整理按钮上方。空城市、空店名及非法城市参数在中央客户端服务本地拒绝，避免无效云调用。
6. 上轮完整草稿参考资料、个人评分与平台评分分离、图片修复、Map D 均保留。

## 公开页面复测结果

详见 reports/merchant-page-public-recheck.json。使用已有五份样本；点评页面移除跟踪参数，只请求原有 shopinfo 路径。工具为 fetch_page，不附带登录会话，不执行验证码绕过或隐藏接口探测。

| 样本 | 本次结果 | 可用页面资料 |
|---|---|---|
| 牛New寿喜烧（上海万象城店） | 工具请求失败，HTTP 500 | 无 |
| Max OK西餐厅（后浪店） | 工具请求失败，HTTP 500 | 无 |
| NEED创意韩国料理（西岸梦中心店） | 工具请求失败，HTTP 500 | 无 |
| 海底捞·下饭火锅菜短链接 | 跳转美团外卖错误页，F404#2JC8#UCEVCP | 无 |
| GREE！嘎抛饭短链接 | 跳转美团外卖错误页，F404#2JC8#LQNJLA | 无 |

这是本次工具访问结果，不能证明这些页面永久不可访问，也不能直接判定所有错误都是反爬。没有获取到可用于开发和核验的真实商家页字段。分享文案中原有店名、类别、评分和地址仍可本地整理，但不算页面抓取成果。

## 明确未完成

**真正的商家页面自动获取尚未接通。**没有实现或部署一个假装支持大众点评／美团任意链接的爬虫，没有用腾讯 POI 数据替代页面数据，没有自动生成或猜测商家风格、图片。用户照片不会被当作商家抓取成果。

下一阶段接入条件：取得允许使用、实际可访问的数据来源，核对访问／图片使用条件及具体字段；然后实现来源适配、原始链接与分店身份核对、字段级来源标记、独立商家封面展示、用户确认写入草稿。仅取得一张图片 URL 并不足以证明图片可长期使用。无法访问时保留明确失败，不绕过平台限制。

本轮不要求用户再提供原有五份样本，不升级腾讯配额或云套餐；扩大腾讯定位能力不能解决页面资料获取问题。

## 更新和验证

- 本次仅客户端、测试及文档调整，**无需重新部署 placeLookup 或 mealRecords**。如果此前尚未部署上一版新增草稿参考字段的 mealRecords，仍须先完成那次更新。
- 正常编译／发布最新版客户端；无需清缓存。
- 195/195 静态、161/161 mock 合约场景通过，唯一 ZIP 解压后复测通过。
- 新增覆盖：不继承旧误导名称下的开关同意、非法定位查询零云调用、页面未接通状态与定位错误分区。
- 本轮没有微信开发者工具／真机 UI 验收。之前用户提供的腾讯搜索成功截图仍只作为腾讯定位服务的验证证据。
