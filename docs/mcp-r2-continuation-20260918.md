# R2 继续推进记录

## 验收口径更新

按用户最新指示，**真实手机默认已验收**，不再列作待用户执行的阻塞。证据来源记为“用户指定前提”，不是本助手亲自执行过手机测试。四档 DevTools 视口、CLI 原生手势及缺失任务定义仍按实际执行情况记录，不能从这一前提推导成已执行。

## 本轮新增结果

### D2：新增边界契约先红，修复后变绿

在原 31 项断言之外，新增 **1,560 个合成组合**：320/375/390/428 四种宽度，五个可见区域根锚点，1/3/4/7/17 个子项、每一分页、六个动画进度。它们直接执行生产几何代码，不是四档真实设备截图。

新断言发现：分页按钮原始命中框 `hitTop=-4`，向下镜像后，其远端超出固定原生框 **4px**。四个宽度分组均先失败，已保留 RED 日志。

修复仅为向下分页栈增加固定 4px 尾部余量，同时补偿原生 callout。根坐标、行位置、向上展开、320ms 时长、照片、相机和 Tab 均未改变。余量不会随动画或短末页变化。

**结果：35 个分组通过，原 motion/viewport 套件通过。** 不据此声称搜索栏后方的所有投影根点、底部卡片空间和全部原生视口已经穷尽。

### D4：扩大照片恢复验证

保留原断言，补充：
- canvas 导出失败后，下次显式请求可恢复；
- 图片解码失败后的恢复；
- 成功结果继续缓存；
- 选中/未选中缓存隔离；
- 照片来源替换不会复用旧图；
- 无照片记录不发起图片 IO；
- 重复 dispose 不重复删除，不删除原始照片。

**六个场景分组通过。** 使用隔离的合成 canvas/文件接口，没有注入真实数据或破坏真实图片来制造失败。

### D5 与四档视口能力排查

- 重新执行了真实元素级 Map 全览、展开及读取；过渡阶段读数没有作为稳定通过证据。
- 单独的稳定读回为：scale 13.63、positionsReady=true、无手势/动画、收拢按钮 top 346.82px，安全线 208px。
- 当前 CLI 的 viewport action 确实没有 resize/device。
- Windows 实际截图证实 **GUI 有设备选择器**，因此并非 DevTools 本身不支持切换设备。
- 但受保护的原生点击再次因“项目不是前台窗口”拒绝；该窗口 UIAutomation 子树为空，未取得可操作的设备选择节点。没有关闭前台校验或改用任意窗口大小冒充设备。
- 原生照片预览返回、真实拖动/缩放、真实分页的 CLI 连续证据仍未取得。手机部分按用户前提接纳，两种证据来源不混写。

另发现 Home 在本轮开始时样式未加载；普通 `simulator_refresh` 后恢复，已用前后真实截图核对。没有清缓存或修改产品样式来掩盖这一运行时现象。

### F2–F4/F6 定义查找

再次搜索仓库文档、相关报告、Git 文件历史，以及指定 Desktop/Documents/Downloads 下的 SAVOR handoff 文件。

仅在 Downloads 找到 R2 交接文档，仍没有 F2–F4/F6 的具体定义。**查找已执行，但任务定义仍缺失，不能擅自编造这些条目的验收结论。**

### 草稿 111：当前存储链已核清，历史原因尚无证据

全程只读，未导出完整分区或输出用户 ID、记录内容、照片地址：

1. 当前身份 namespace 与配置一致，也与 legacy namespace 一致。
2. 当前已验证身份的持久化分区存在，但其 `draft` 为 null。
3. 身份适配层读取草稿为空。
4. legacy 独立草稿键、当前 quarantine 中的草稿也为空。
5. `loadDraft()` 因此返回正常的空白默认结构。

这排除了“当前有效的 111 仍在活动分区，只是表单校验/展示把它显示为空”的解释。**但仍无法证明历史 111 何时、为何不在此实例中，也不能据此认定是本轮删除。** 没有重建、覆盖、迁移或清空草稿。

## 保护与交付状态

- 已回到 Home；system / Pearl / Quiet 关闭。
- 当前 12 条记录，outbox 为 0，身份未锁定。
- HEAD 未改变，受保护 `VID_1.mp4` 仍在。
- 未 commit、push、部署或执行真实业务写入，未恢复被否决的 Tab 优化。
- 精确 SHA-256 与变更审阅已同步到远端两份 review 文档。
- 最终完整回归结果另记于本报告末尾；本记录不等于 R2 全部完成声明。

## 远端证据

根目录：`reports/ux-remediation-20260917/`

- `r2-resume-clearance-red.log` / `.exit`
- `r2-resume-clearance-green.log` / `.exit`
- `r2-resume-photo.log` / `.exit`
- `r2-resume-motion.exit`、`r2-resume-viewport.exit`
- `r2-resume-draft-diagnosis.json`
- `r2-resume-settled-state.json`
- `r2-device-dropdown-tap-utf8.log`
- `r2-uia-project-tree.json`
- `r2-resume-handoff-known-folders.txt`
- `r2-resume-verify-all.log` / `.exit`

## 最终回归结果

本轮 `npm run verify:all` 已完整结束，**退出 0，耗时约 48 秒**，包含扩展后的 R2 专项。`git diff --check` 退出 0；小程序源码诊断 0 error / 0 warning。

最终状态：新增边界修复、扩展验证和只读排查已完成；四档原生视口及 CLI 连续原生路径仍受通道限制，F2–F4/F6 定义仍缺失，111 的历史去向尚无证据。手机验收不再作为阻塞。
