# R5 requirement reconciliation and progress — 2026-09-19

P2-02 Memory identity, P2-04 Cloud tools summary, P2-05 empty Sync retry hierarchy and whole-map veil removal already existed before this continuation. Read current implementation and five-case green R4 contract; do not claim these as newly authored here. P2-07 Preferences remains a visual candidate, not a proven contrast failure. A/C/D/E/F1/F5/F7 previous scoped evidence remains valid; no fabricated backend, photo or draft data.

The current red baseline revealed three independent issues: wrong WXML review hashes omitted final LF (proven byte-for-byte), unregistered narrow CSS refinements, and the old veil-existence contrast contract. Review metadata/projection now preserve exact byte guards and unchanged legacy assertions; new runtime absence/tamper tests are added. Full baseline then reached an existing feedback-policy failure: the local refinement replaced the explicit stored branch. Restore only the stored cloud-only explanatory label in the job row while retaining human-readable labels for other states. Existing verify-feedback-policy is the prior RED contract; no change to policy tests, consent, handlers, service or network calls.

Workspace WXML post-repair exact version: sha256:08b1de0bf4518161e74e2012d1d443801424e5dded3308c0df295c7fb811c28f

Draft: executed the historical 081 read-only function again. Partition exists, owner/namespace checks and chunk integrity pass, persisted draft absent. Historical 081 proves 111 existed then, not that the current account is identical across time (raw ID was intentionally not captured). No writes or recovery claims. No new evidence identifies a permissible backup to restore.

Next: wait for complete baseline, isolate paced native map gestures with fresh viewport and stable foreground, then genuine sheet/workspace/pending-menu interaction and affected configuration captures. Failed selectors and no-movement inputs are not success evidence.

## R5 final verification 2026-09-19

- verify:all exit 0 after byte-review reconciliation and workspace stored label fix.
- diff-check exit 0, icons exit 0.
- verify-r4-local-refinements 5 PASS retained, plus verify-map-veil-review PASS for runtime absence and tamper rejection.
- Native captures: Home390, Map390 with veil absent, Memory sheet showing raw restaurant '食堂！' before photo, Workspace showing status summary before tools, Sync empty secondary-button contract verified via data, Sheet close without mutation.
- Draft: historical 081 present true, current readonly partitionPresent true, owner/namespace true, chunkIntegrity passed, persistedDraft present false. No write, no recovery claim, no cross-time identity proof.
- Map gesture: guarded native drag dispatched with foreground and same-process ownership checks, but getRegion timed out and page scale remained 18; not claimed as movement success. Need fresh viewport test next round.
- Preferences chips: selector search found no .chip elements; pending visual candidate remains unverified, not modified.
- Pending pagination: contract test covers 0/1/6/8/17 reachable, <=6 entries, reversible paging, stale identity blocked; no real chooser save.
