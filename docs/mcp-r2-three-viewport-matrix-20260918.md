# R2 多视口推进记录

**当前结果：三档真实视口的采集矩阵完成，共 144 项；R2 尚未全部闭环。**

任务依据以用户指定的 `SAVOR-HANDOFF-20260917-R2.md` 为准。真实手机按用户指定前提视为已验收；不将此写成本助手亲测，不再追索旧任务书文件名。

## 1. 新通道恢复与断点续跑

- 新 MCP 地址初始化成功。
- 连接时现场为 iPhone 12/13 (Pro)、390×844、英文/Pearl/Quiet 开启，页面为 Add。
- 先恢复 system/Pearl/Quiet 关闭并返回 Home；恢复前后记录、outbox、草稿均比较一致。
- 390 档从 33/48 项断点续跑。一次截图响应尚未完整返回时脚本停止，没有把空结果计为成功；将测试客户端的命令等待窗口扩展为 60 秒后重跑缺失项，完成 48/48。

## 2. 三档真实视口完整采集

| DevTools 实际机型 | 每页实际查询的窗口尺寸 | 已确认截图 |
|---|---:|---:|
| iPhone X | 375×812 | 48/48 |
| iPhone 12/13 (Pro) | 390×844 | 48/48 |
| iPhone 12/13 Pro Max | 428×926 | 48/48 |

每档覆盖：中文/英文 × Pearl/Dusk × Quiet 开/关 × Home、Map、Add、Us、Me、Reports。

机型通过真实设备菜单选择；不是 wx API mock、Page 数据伪造或桌面窗口 resize。每次采集都校验实际窗口尺寸、路由、locale/theme 及适用页面的 Quiet 状态。偏好通过集中式 store API 控制，每次变更核对记录、outbox、草稿不变；不是 Settings 菜单真人点击验收。

375 档切换后第一次诊断调用超时，不能算确认成功；后续 48 项逐页实际尺寸/状态校验均通过，采集才被记录。没有把该失败调用写成成功。

**144 项是完整采集，不是全部截图已逐张目视验收。** 本轮抽查了 375 英文/Dusk Home、428 英文/Dusk Us、390 英文/Dusk Reports，以及新 Memory 截图：可见对应语言与主题、单复数/Us 独立说明块、英文报告海报及实际照片。

## 3. 320 档与机型恢复仍未闭环

此前已确认 iPhone 5 为真实 320×568，并采集 8 张；该批证据独立保留，不冒充本轮 48 项完整矩阵。

本轮尝试再次切入 320 档时，前台校验开始持续失败。只读探针显示，前台窗口进程与目标开发者工具进程不同；正常窗口激活尝试没有使实际 foreground 匹配。所有失败步骤都在发送点击前停止。

因此：

- 320×568 完整矩阵尚未补齐。
- 尚未成功将设备恢复为原始 390×844。
- **当前实际设备停在 iPhone X、375×812。** 不将当前机型描述成已恢复。

## 4. D5 新执行的连续片段

真实元素操作连续完成：Map 展开 → 选择子项 → 全览 → 打开 Memory。

前后读回确认：选中 ID 不变，卡片与选中 ID 对应。Memory 截图可见原记录照片，阅读从上方开始。之后返回 Home。

原生照片预览返回、真实拖动/缩放和真实多页栈完整往返，仍没有本助手的新完整证据，不能用本片段替代。未调用保存、点赞、绑定或导出等真实业务动作。

## 5. 源码与验证

本轮未修改产品源码或业务测试断言。结束时发现 Map JS 相较上一轮审阅快照已有一段更新：全览保留选择时同时判断 filtered markerGroups 中的子项，而不是仅看根 marker 的 visibleIds。本轮没有覆盖或回滚该更新，也不将其归为本轮编写。

当前读回 Map JS SHA-256：

`b873f86b93c0dae3c86eaab7de365351d00553e9b8fb3fa3f3b020b6965eeb7c`

该哈希为本轮结束时读取；未在每一批采集前后逐次取源码哈希，因此不宣称全部历史截图均来自完全相同的构建。

结束时重新执行：

- **完整 `verify:all` 退出 0**。
- 小程序诊断：0 error / 0 warning。
- diff 检查命令退出 0。

没有 commit、push、部署、清缓存、身份绕过、照片删除或真实业务数据写入。

## 6. 最后确认的现场

- 页面：Home。
- 机型：iPhone X，375×812（未恢复原机型）。
- 设置：system / Pearl / Quiet 关闭（已恢复）。
- 记录：12；outbox：0。
- 草稿 111：仍未在当前实例中发现；没有补造或覆盖。
- 没有在后台继续运行的矩阵任务。

## 7. 持久证据

本地：`mcp/four375-results.json`、`mcp/four390-results.json`、`mcp/four428-results.json` 及对应运行日志。

远端 `reports/ux-remediation-20260917/`：

- `r2-three-full-viewports-manifest.json`：144 项记录及此前 320 档的独立部分证据。
- `r2-four375-*`、`r2-four390-*`、`r2-four428-*`：逐项设置比较、导航、真实页面状态与截图。
- `r2-new-chain-*`：新的 Map 连续片段。
- `r2-four-matrix-verify-all.log` / `.exit`：最新完整回归。
- `r2-three-viewports-final-state.json`：最终现场读回。

剩余：320 档完整矩阵、机型恢复、全部截图目视复核及尚缺的原生连续路径；R2 中未展开定义的条目仍不自行编造通过结论。
