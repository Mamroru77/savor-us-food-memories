# R2 剩余阻塞复查

## 本轮结论

已再次尝试，但两项尚未闭环。没有修改产品行为或降低验收标准，也没有将“真实手机默认已验收”推导成四档 DevTools 矩阵已执行。

### 1. 原生操作：成功打开设备菜单，后续焦点被其他应用占用

这次并非原样重复上一轮：

- 在正常恢复目标窗口后申请前台，保留实际 foreground、坐标范围及遮挡校验。
- **一次原生点击成功，已用 Windows 截图确认设备菜单打开**，能看到“机型 / 显示比例 / 字体大小 / 自动热重载”。
- 随后的子菜单操作因遮挡或前台不匹配被拒绝。
- 增加只读窗口归属探针后，确认当时前台窗口和目标坐标下的窗口属于另一个应用（微信），不是微信开发者工具。输入未发往该窗口。
- 对原生菜单的支持仅允许目标项目窗口或经过 owner 链和进程 ID 双重验证的同进程自有弹窗；没有取消安全校验。去掉固定等待的尝试还增加了点击前的即时 foreground 复核，也未对错误窗口发出输入。

因此已证实设备选择 GUI 能够通过正常原生点击打开；但**尚未选择新机型，没有四档视口或新增原生拖动/缩放/预览返回证据**。

继续执行需要一段可用的桌面操作窗口：目标 `savor-audit-repairs-20260915` 微信开发者工具处于前台，期间不由其他应用占据前台或遮挡模拟器。不是要求关闭安全校验。

### 2. F2–F4/F6：新增压缩包内部检索，仍没有定义

扩大了文件名搜索范围，检查 SAVOR、任务书、验收相关的 Markdown、文本、PDF、Word 和 ZIP 候选。不存在的候选目录使 find 返回非零，故不把该命令当作无遗漏的全盘搜索证明。

发现桌面的 `savor-audit-repairs-20260915.zip` 后，只读扫描了其中 **4,728 个符合条件的文本条目**（md/txt/json、单项不超过 2MiB，排除 node_modules/.git；未解压覆盖仓库）。

有效相关命中只有旧 inventory：

- F2：【无目录】
- F3：【无目录】
- F4：【无目录】
- F6：【无目录】

其余命中是编码或图片数据中的字符，不是验收条目。结合此前仓库、Git 文件历史和已知文档目录检索，当前可访问材料中仍未找到原始定义。不能从“无目录”推断任务含义或默认通过。

继续完成需要原任务书，或 F2、F3、F4、F6 四项原文/截图。无须重发整个历史项目。

## 最终保护核对

- 本轮没有产品源码或测试修改；Map JS 与两个边界测试的 SHA-256 与上一轮审阅值一致。
- 未重新执行完整回归；最近一次完整回归仍是上一轮的 `r2-resume-verify-all.exit = 0`。
- 本轮 diff 检查无输出；HEAD 保持 `2b162558deca71e68021d89e2a4005b359bb540c`。
- `VID_1.mp4` 仍存在；没有提交、部署或业务数据写入。
- 只读最终状态：Home、390×844、12 条记录、outbox 0、system/Pearl/Quiet 关闭。未变造草稿 111。

## 证据

远端 `reports/ux-remediation-20260917/`：

- `r2-retry-device-utf8.log`：成功通过目标窗口前台检查并发送原生点击。
- `r2-device-submenu-utf8.log`：遮挡检查拒绝后续点击。
- `r2-owned-menu-2-utf8.log`：前台检查拒绝。
- `r2-window-ownership-probe.ps1`：只读窗口 owner/PID 探针；执行结果确认前台与坐标下均为其他应用。
- `r2-retry-archive-definition-hits.json`：4,728 项归档扫描结果。
- `r2-retry-final-readonly.json`：最终运行状态。

本地 `mcp/retry-device-open.png` 保存设备菜单确已打开的截图。
