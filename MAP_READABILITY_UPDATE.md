# 地图重叠、缩放过渡与卡片调整

基线：`cb3f591`。用户截图显示照片标记重叠，并要求平滑缩放、收起卡片、增大卡片照片。本轮只调整客户端 Map，不改 Me、云函数或记录坐标。

## 1. 重叠标记

- 按地图当前 scale 将坐标投影到屏幕像素近似空间，结合 80px／48px 标记的占用范围分组，而非使用固定公里数。
- 靠近的记录只显示一个代表标记和数量角标；若组内包含当前选中记录，代表标记使用它的照片和真实坐标。
- 点击代表标记或名称 callout，打开可滚动的“附近的记录”列表。店名与日期同时展示，可区分同店不同次回忆。
- 数字是记录数量，不承诺它们一定是不同门店。超过六条也不会截断；同坐标记录可以逐条选择。
- 放大地图到图标足以分开时重新展开；缩放过程中不会为了分组额外居中。
- 不移动、伪造或持久化任何餐厅坐标；组合标记沿用代表记录的位置。

当前分组使用常规平面地图的 Mercator 投影近似，并非原生地图渲染完成后的逐像素碰撞检测。旋转／倾斜、原生标签实际尺寸和不同设备上的边缘重叠仍需真机检查。

## 2. 选中尺寸过渡

- 约 200ms ease-out 缓动，约每 25ms 更新宽高。
- 新选中标记由 48px 过渡到 80px；仍独立显示的原选中标记反向缩小。分组中被收进代表标记的图标不强行留在地图上做退出动画。
- 锚点保持在尖端，尺寸过渡不改变坐标。
- 快速切换／重新分组会取消旧动画；离开地图停止定时器。
- “减少动态效果”开启时不执行尺寸动画。
- 已合成并缓存的同状态照片可立即复用，避免每次刷新先变回占位图；第一次加载或失败时仍使用既有占位机制。

原生 map 的最终帧率和尺寸更新表现尚未真机验收；本地测试证明的是调度和状态正确，不代表原生动画已实测流畅。

## 3. 卡片

- 卡片右上边缘增加小型收起按钮。
- 收起后保留底部店名小条和展开箭头，点击即可恢复；不改变当前选中、收藏或地图视野。
- 选择另一条记录时重新展开卡片。
- 卡片保持 **340rpx** 总高，图片保持 **212rpx** 宽，高度改为 **285rpx**；距顶部 30rpx、底部 25rpx。
- 文案区域从顶部布局，照片上边对齐店名行框，下边延伸到“校正位置／在腾讯地图打开”操作行附近。
- 图片独立于上方文字区域，点击图片仍打开原详情；操作行不被图片遮挡。
- Me 高度、五 Tab 和主题体系不变。

## 4. 验证

**193/193 静态检查、125/125 mock 场景通过**，较上一版新增 6 个回归场景。

新增覆盖：不同缩放下聚合／拆分、坐标不变、日期变更线附近投影、同坐标九条记录可选、旧缩放回调不覆盖新状态、收起展开无业务副作用、缓动起终点、减少动态效果和隐藏时取消动画。

原有照片缓存与48条增强预算测试保留。测试照片预算／旧图防覆盖时使用彼此分开的坐标，避免新增分组机制改变这些测试的前提。旧图片 215rpx 布局断言按本次用户要求更新为 285rpx，并继续检查上下间距；没有关闭原测试或删除基线核验。

## 5. 使用与验收

本轮无云函数变更，只需使用最新包重新编译前端，无需清空缓存、草稿或数据库。

真机重点：
1. 截图中的相邻标记是否显示数量且可分别选取；放大后是否分开。
2. 同坐标、密集地点、不同缩放和旋转情况下的标记及数量标签是否清晰。
3. 两店间快速切换时尺寸过渡是否流畅，是否出现错图；开启减少动态效果再对比。
4. 收起／展开可点到，图片与店名及底部操作行的视觉对齐；中英、深浅主题、小屏幕均需检查。

本轮没有代替用户执行微信开发者工具／手机上的原生地图测试。

## 后续布局修订

用户随后要求名称移至标记下方、收起按钮移入卡片、右图改正方形。当前右图尺寸是280×280rpx，布局以 MAP_CARD_ALIGNMENT_UPDATE.md 为准；上面的285rpx图高为上一版记录。
