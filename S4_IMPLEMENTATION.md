# S4：一次完成剩余四类候选实现

日期：2026-09-14。基于当前保留的 `fix-9.13 @ 4daeb2e` 工作树，叠加 S1/S2/S3，不替换工程、不恢复 K1。

**状态：候选代码与自动化完成，未部署、未真实投递、未原生/真机验收。** 这不是四次交付，也不需要先让用户逐类测试才能继续。

## 入口与文件

- Me → **云端工具 / Cloud tools**：备份、导入预览与恢复、云档案/偏好、一次性提醒、反馈、任务列表。
- Me → **年度报告 / Annual reports**：年度统计、全年月分布、里程碑、HTML 与仅汇总海报。
- Help & Feedback 保留原来的本机反馈笔记，新增进入云端反馈的按钮；不把旧本机笔记标为已投递、不自动上传历史笔记。
- 保持五个 Tab；新页面均为非 Tab。未修改正式 Tab 交接机制、地图动画/位置、Home/Add 布局。
- 后端：`cloudfunctions/workspace/{handler,index,schema,image}.js`。
- 前端边界：`utils/workspace.js`；统计：`utils/annualReport.js`；原生页：`pages/workspace`、`pages/reports`。
- `identity` 增加当前用户分区内的 `workspaceIntent` 与 `workspaceDraft`，不触碰兼容旧键，不自动重放。

## 6. 云端批量备份 / 导入 / 恢复

### 实际覆盖

- **云端备份是本机日记的结构化快照**，含照片引用，不含照片二进制。先点“只读刷新云日记”可纳入当前云端日记；这不是服务端全库时点快照。
- 最多 500 条/任务，5 条/块，每块原始 JSON ≤48 KiB；整体云请求 ≤128 KiB。
- 输入支持云备份 `rows`、既有 `{memories}` / Memory 数组和当前私人分区备份内的 `partition.diary.memories`。不执行备份内 outbox、身份、draft 或共享授权。
- 选择 JSON 后，先在当前用户私有目录保存**未修改的原文件**，再保存规范化操作源文件，最后开始网络写入。
- `createTask → putChunk* → sealTask` 只建立不可变快照，不创建日记。预览显示总数、块数、照片引用剥离数量及首块样本；不是宣称逐条人工审阅了全部内容。
- `commitTask` 必须携带当前 manifest 和明确同意。`stepTask` 每次事务处理一块，记录 cursor / created / skipped；网页/进程重开后可从任务列表继续。
- 原始块摘要不可替换；恢复前再次核验块内容与规范化数据一致性。
- 重试、并发重复 step、丢响应不重复写。导入行 ID 为用户范围内的稳定摘要；已存在行（包括删除标记）不覆盖、不复活。自己的原 cloudId 已存在则跳过。
- 外部/其他账号 cloudId 不是读取或覆盖权限。创建私有副本时重新绑定实际 OPENID，成员仅本人、coupleId 为空、shared=false、照片/地点照片引用不恢复；不把不明坐标当作已确认位置。
- 取消保留已经写入的日记和已上传快照，不回滚、不物理删除。完成任务不能被“取消”伪装成已撤销。
- 云任务列表可分页、导出完整已上传的 JSON。若本机源文件丢失且上传未完，只能保留/取消未完成任务，不能凭空补齐内容。

### 边界

这不是照片文件灾备、覆盖式回滚或全库一致性导出。恢复本机未同步记录时，原本机条目继续保留，可能与新云副本并存；不偷偷删除本机源记录。云端已删除的同一目标也不会被该任务复活。需要媒体重新授权/上传的情形仍走既有流程。

## 7. 云档案 / 偏好与最小成员资料

- 先读云版本，再明确选择上传本机或采用云端。上传需要 expected revision；同请求回执可恢复，修改请求内容拒绝，版本冲突不会自动覆盖。
- 上传字段：姓名、简介、饮食、菜系、privateByDefault、showLocations、reminders 偏好。
- 主题、语言、减弱动效、页面自定义标题、partner/togetherSince、通知已读等不在此云同步白名单，保留本机值。
- 头像必须重新选择缩略图，客户端按比例压缩到 ≤256 边；服务器验证 JPEG/PNG、剥离元数据，限制 ≤64 KiB / ≤512 边。拒绝用任意云 fileID/URL 认领头像。
- 默认只本人可读。采用云端之前保存整个当前分区的本机 before-image；本机 profile/settings 一次提交，保留关系字段与设备偏好。云端没头像时保留本机头像。
- **最小伙伴资料投影**：另行明确授权，仅展示云端姓名、简介，不含头像/偏好/OPENID；授权绑定 spaceId + membershipVersion + profile revision。
- 撤回、退出、成员版本变化、重新上传云档案均不自动续授。读取方最多展示 15 秒，页面隐藏/身份变化清空。已经截屏/抄录的内容不能收回。
- 同步 `reminders=true` 不产生任何微信订阅授权或发送任务。

## 8. 年报 / 里程碑 / 分享

- 全部基于当前账号本机真实日记；去重并排除样例、删除、待删除及截至日期之后的记录。
- 统计条数、记录日、已确认地点、未知地点、最长连续天数、月分布；当年未来月份显示未到来，不伪造为已完成统计。
- 仅使用可辨认的个人单评分，不把旧双人均分/旧双人分数字段当作本人评分；不推算总消费或双人身份。
- 第 1/10/25/50/100/250/500/1000 餐里程碑依真实日期排序，展示所选年度达到的累计门槛。
- 默认 HTML 只含汇总和里程碑日期。另行勾选才包含餐厅、日期与已确认城市；始终不含姓名、笔记、照片。
- Canvas 海报固定仅汇总；支持原生分享图片菜单，低版本退回预览。HTML 自包含、无外部资源，用户内容做 HTML 转义。
- 生成统计不写日记、不读取伙伴、不上传报告。导出/分享由用户点击；分享后副本不可远程撤销。
- 新报告页以中文为主；部分任务技术状态保留英文。不能把本轮称为全语言/原生视觉验收通过。

## 9. 实际提醒 / 反馈投递

- 真正的生产适配器已经接入 `cloud.openapi.subscribeMessage.send` 与运营 HTTPS webhook，不是只有本机 toast。
- **一次性提醒**：用户手势直接调用微信订阅授权，只有当前模板返回 accept 才创建任务；保存实际本人收件人、模板、时间/时区和内容。最多 10 条未终结提醒；不是无限循环订阅或硬实时闹钟。
- **反馈**：私有云端工单 + 可选联系信息。未配置渠道时保留 `blocked_config`，不假装送达；本机旧反馈不自动迁移。
- 工作器仅接受实际 allowlist 管理员，或无实际 OPENID 且常量时间校验通过的 ≥32 字符服务端密钥。伪造 Timer/admin/event.OPENID 无权。
- 定时器 `Message` 只用作受限 JSON 路由，仍验证服务端密钥。不能单凭事件 Type 获取管理权限。
- 事务抢占发送租约，发送在事务外；重复工作器不会并发重复发送。单次最多扫描 20 项、约 40 秒后停止继续抢占；单项传输超时 10 秒，余项下次处理。
- 状态：`queued / blocked_config / sending / sent / failed / uncertain / cancelled`。
- `sent` 只表示平台/渠道正向确认，不是用户已收到/已读或客服已处理。
- 丢响应、超时、发送后崩溃记为 `uncertain`，不盲目重发微信一次性消息。发送中不可“取消成功”。
- 反馈未知结果重试只有在运营方明确验证 endpoint 按 ticketId/Idempotency-Key 去重，并设置 `FEEDBACK_IDEMPOTENT=true` 后才允许。
- 实际管理员可用 `resolveJob` 在发送租约过期/未知状态下，根据证据记录 sent/failed；持久化真实管理账号哈希、证据和时间。此操作不重新发送。
- 本机反馈/提醒填写草稿也按用户分区持久化，换号不泄漏。

## 自动化与限制

- `verify-workspace.cjs`：48 项后端/实际客户端模块/页面授权/报告检查。
- `verify-workspace-adapter.cjs`：5 项生产 index 适配器线格式、订阅返回、HTTPS ack 和 Timer 路由检查。
- 独立云函数不能运行时 require 兄弟目录：schema/image 是可独立部署的精确副本；自动测试要求与 canonical 文件字节一致，防止版本分叉。
- 网络、CloudBase、微信原生接口用 mock 替代；没有真实投递、云部署、权限变更、实际数据删除或 Git push。
- 完整证据以 `CURRENT_TEST_RESULTS.txt` 为准；最终集中执行事项见 `UNIFIED_ACCEPTANCE.md`。
